/**
 * Globe — the dashboard's alias for lucide's Globe icon, kept so call sites
 * do not churn. Rendered through lucide so the Boxy stroke rules (square
 * caps, mitred joins, 1.5px) in tailwind.css apply to it like every icon.
 */
export { GlobeIcon as Globe } from "lucide-react";
