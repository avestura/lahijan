Implement **{{WS_ID}}** ({{SLUG}}) following the standard ws-implementer checklist.

Context for this autonomous run:
- Repo root:   {{REPO_ROOT}}
- Branch:      {{BRANCH}}  (already checked out; commit your work here)
- WS doc:      {{WS_DOC_PATH}}
- Main has WS-01 and WS-02 already merged (AGENTS.md, ADRs, skills, Makefile,
  CI, opencode.json, all WS docs).

Procedure:
1. Read /AGENTS.md (the project heart).
2. Read {{WS_DOC_PATH}} end to end.
3. Read every file under that doc's "Required reading" list.
4. Read docs/architecture/conventions.md (especially your area's section).
5. Read the relevant .opencode/skills/<area>/SKILL.md.
6. Implement one concern at a time, with tests.
7. After every meaningful change, run: make lint test
8. Commit with conventional-commit messages like "feat(db): ..." or "feat(api): ...".
   Use the scope that matches the area: compute, dns, s3, auth, rbac, billing,
   plugins, api, web, website, docs, ci, build, conf.
9. Tick every Definition of Done box in the WS doc. If a box can't be ticked,
   explain why in your final message.
10. Update the WS doc's Status field to "done" (or "blocked" with reason).

Do NOT:
- Merge to main (the runner will do that after verifying).
- Push to remote (local only).
- Skip tests or lint.
- Edit any ADR that is already Accepted; write a new one if you need to.

When you finish successfully, print this exact line on its own line:
WS_IMPLEMENTATION_COMPLETE: {{WS_ID}}

If you get blocked, print this exact line on its own line:
WS_IMPLEMENTATION_BLOCKED: {{WS_ID}} REASON_HERE

Begin.
