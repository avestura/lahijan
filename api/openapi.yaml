# Lahijan Cloud Platform - OpenAPI 3.1 specification
#
# This file is the SINGLE SOURCE OF TRUTH for Lahijan's public HTTP API.
# Generated code (Go server types, Go client SDK, TypeScript schema + client)
# is produced from it by `make openapi-gen`. CI fails if the spec drifts from
# the committed generated code (see .github/workflows/openapi.yml).
#
# Rules (see ADR-0015 and docs/architecture/conventions.md#http-api):
#   - All public routes live under /api/v1/.
#   - Every error response uses the standard Error envelope.
#   - Backend infrastructure names (Incus, PowerDNS, SeaweedFS) NEVER appear
#     here; use the user-facing terms (compute / dns / object storage).

openapi: 3.1.0
info:
  title: Lahijan Cloud Platform API
  version: 0.1.0
  description: |
    Self-service API for compute, DNS, and object storage. Lahijan wraps the
    operator's infrastructure and exposes a single, tenant-scoped surface.

    All timestamps are RFC 3339 UTC strings. All ids are UUIDs.
  contact:
    name: Lahijan Maintainers
    url: https://github.com/avestura/lahijan
  license:
    name: Apache-2.0
    url: https://www.apache.org/licenses/LICENSE-2.0

servers:
  - url: /
    description: Same-origin (served behind the dashboard).

# ---------------------------------------------------------------------------
# Conventions for tags: one per user-facing module. Infrastructure-level
# endpoints (health, ping) use the "platform" tag.
# ---------------------------------------------------------------------------
tags:
  - name: platform
    description: Infrastructure endpoints (health, liveness, ping).
  - name: auth
    description: Authentication and the current user.
  - name: audit
    description: The append-only audit trail of privileged actions.
  - name: admin
    description: Platform-admin endpoints (cross-tenant; platform.admin role only).
  - name: compute
    description: Compute instances, images, profiles, networks, and storage volumes.
  - name: billing
    description: Billing & metering (balance, ledger, usage, receipts, price catalog).

# ---------------------------------------------------------------------------
# Paths
# ---------------------------------------------------------------------------
paths:
  /health:
    get:
      tags: [platform]
      operationId: getHealth
      summary: Service health probe
      description: |
        Returns the service health. Intended for orchestrators and load
        balancers; not a privileged endpoint.
      security: []
      responses:
        "200":
          description: Service is healthy.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Health"
        "503":
          description: Service is degraded.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /api/v1/ping:
    get:
      tags: [platform]
      operationId: ping
      summary: Round-trip probe for the API pipeline
      description: |
        Returns a pong with the current server timestamp. Proves the OpenAPI
        -> oapi-codegen -> Fiber pipeline is wired end to end. Emits an
        OpenTelemetry trace span.
      security: []
      responses:
        "200":
          description: Pong.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Pong"

  /api/v1/auth/register:
    post:
      tags: [auth]
      operationId: register
      summary: Register a new user account
      description: |
        Creates a user with an argon2id password hash, opens a session, issues a
        refresh token (set as cookies), and emails a verification link. The
        response returns the new user; the session cookie and refresh-token
        cookie are set on the response.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
      responses:
        "201":
          description: Account created; session cookies set.
          headers:
            Set-Cookie:
              $ref: "#/components/headers/SetCookie"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/Conflict"

  /api/v1/auth/login:
    post:
      tags: [auth]
      operationId: login
      summary: Log in with email and password
      description: |
        Verifies credentials and opens a session. Sets the session and
        refresh-token cookies; returns the authenticated user.

        When MFA is required (the user has at least one enrolled factor OR
        any of their tenant memberships has mfa_required = TRUE), the
        response is 202 Accepted with a pending_session_token instead of
        200 OK with a session. The caller then completes the MFA challenge
        at /api/v1/auth/mfa/challenge to obtain the real session.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Logged in; session cookies set.
          headers:
            Set-Cookie:
              $ref: "#/components/headers/SetCookie"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "202":
          description: |
            MFA challenge required. The pending_session_token is also set
            in a short-lived cookie (lahijan_mfa_pending) for browser
            clients. Exchange it for the real session at
            /api/v1/auth/mfa/challenge.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MFAChallengeRequired"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/logout:
    post:
      tags: [auth]
      operationId: logout
      summary: Log out the current session
      description: |
        Revokes the session and every refresh token in its family, then clears
        the session and refresh cookies. Idempotent.
      security: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LogoutRequest"
      responses:
        "200":
          description: Logged out.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/refresh:
    post:
      tags: [auth]
      operationId: refresh
      summary: Rotate the refresh token
      description: |
        Consumes the presented refresh token (from the cookie or body) and
        issues a new one in the same family. Reusing a previously rotated
        token revokes the whole family and the session (reuse detection) and
        returns 401.
      security: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshRequest"
      responses:
        "200":
          description: Refreshed; new cookies set.
          headers:
            Set-Cookie:
              $ref: "#/components/headers/SetCookie"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/verify-email:
    post:
      tags: [auth]
      operationId: verifyEmail
      summary: Confirm an email verification token
      description: |
        Consumes the single-use token emailed at registration (or via
        resend-verification) and marks the user's email verified.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TokenRequest"
      responses:
        "200":
          description: Email verified.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/auth/resend-verification:
    post:
      tags: [auth]
      operationId: resendVerification
      summary: Resend the email verification link
      description: |
        Issues a fresh verify-email token and emails the link. Returns the same
        response whether or not the email is registered (no enumeration).
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailRequest"
      responses:
        "200":
          description: Verification email sent (or would be sent).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /api/v1/auth/password-reset/request:
    post:
      tags: [auth]
      operationId: requestPasswordReset
      summary: Request a password-reset link
      description: |
        Emails a single-use reset link to the user if the account exists.
        Always returns 200 (no account enumeration).
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmailRequest"
      responses:
        "200":
          description: Reset email sent (or would be sent).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"

  /api/v1/auth/password-reset/confirm:
    post:
      tags: [auth]
      operationId: confirmPasswordReset
      summary: Reset the password with a token
      description: |
        Consumes the single-use reset token and sets the new password. The new
        password is strength-validated; the consumed token cannot be reused.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PasswordResetConfirmRequest"
      responses:
        "200":
          description: Password reset.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/auth/me:
    get:
      tags: [auth]
      operationId: getCurrentUser
      summary: Current authenticated user
      description: Returns the user identified by the session cookie or PAT.
      responses:
        "200":
          description: The current user.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"
    patch:
      tags: [auth]
      operationId: updateCurrentUser
      summary: Update the current user's profile
      description: |
        Updates display name and/or locale and/or password. A password change
        requires currentPassword. An email change requests a confirmation
        email and does not swap the email until confirmed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMeRequest"
      responses:
        "200":
          description: Updated; the new user state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/personal-access-tokens:
    get:
      tags: [auth]
      operationId: listPersonalAccessTokens
      summary: List the current user's PATs
      description: Returns the caller's non-revoked personal access tokens.
      responses:
        "200":
          description: The caller's PATs.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PersonalAccessToken"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [auth]
      operationId: createPersonalAccessToken
      summary: Issue a personal access token
      description: |
        Mints a PAT with the requested scopes and expiry. The raw token is
        returned exactly once in the response body; only its hash is stored.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePersonalAccessTokenRequest"
      responses:
        "201":
          description: The new PAT (raw token included).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonalAccessToken"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/auth/personal-access-tokens/{tokenId}:
    delete:
      tags: [auth]
      operationId: deletePersonalAccessToken
      summary: Revoke a personal access token
      parameters:
        - name: tokenId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Revoked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # -------------------------------------------------------------------------
  # External identity providers (WS-07a): OAuth 2.0 social login + OIDC.
  # -------------------------------------------------------------------------
  # Each provider has a start + callback pair. The start endpoint mints a
  # signed state token + PKCE verifier, sets them as short-lived cookies, and
  # 302s the browser to the IdP. The callback verifies the state, exchanges
  # the code for tokens, fetches the profile, and either logs the user in or
  # links the identity to the logged-in user (via the lahijan_link_uid cookie
  # set by the start endpoint when called from an authenticated session).

  /api/v1/auth/oauth/{provider}/start:
    get:
      tags: [auth]
      operationId: startOAuth
      summary: Begin an OAuth 2.0 social-login flow
      description: |
        Mints a signed CSRF state token + PKCE verifier, sets them as
        short-lived cookies, and 302s the browser to the IdP's authorization
        endpoint.

        When called from an authenticated session (the user is linking a new
        IdP to their existing account) the response also sets a
        lahijan_link_uid cookie carrying the user's id; the callback enforces
        the link against the user who started the flow.
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          description: The configured OAuth provider key (e.g. google, github).
          schema:
            type: string
      responses:
        "302":
          description: Redirect to the IdP authorization URL.
          headers:
            Location:
              description: The absolute IdP authorization URL.
              schema:
                type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          description: The provider key is not configured or not enabled.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  /api/v1/auth/oauth/{provider}/callback:
    get:
      tags: [auth]
      operationId: callbackOAuth
      summary: Finish an OAuth 2.0 social-login flow
      description: |
        Consumes the IdP's redirect (?code + ?state), verifies the state
        against the cookie nonce, exchanges the code for tokens (with PKCE),
        fetches the profile, and either:
          - logs the user in (sets the session + refresh cookies), or
          - links the identity to the logged-in user (when lahijan_link_uid
            is set), or
          - returns a "completion required" envelope when the IdP returned no
            email and no user could be created.

        On success the browser is redirected to the dashboard root (`/`); on
        failure it is redirected to the login page with an error query.
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          schema:
            type: string
        - name: code
          in: query
          required: true
          schema:
            type: string
        - name: state
          in: query
          required: true
          schema:
            type: string
      responses:
        "302":
          description: Redirect to the dashboard or login page.
          headers:
            Location:
              schema: { type: string }
        "400":
          $ref: "#/components/responses/BadRequest"

  /api/v1/auth/oidc/{provider}/start:
    get:
      tags: [auth]
      operationId: startOIDC
      summary: Begin an OpenID Connect flow
      description: |
        Same cookie + state pattern as /auth/oauth/{provider}/start but for
        OIDC providers. Mints the PKCE verifier AND the OIDC nonce in addition
        to the state token; both are persisted as short-lived cookies.
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          schema: { type: string }
      responses:
        "302":
          description: Redirect to the IdP authorization URL.
          headers:
            Location: { schema: { type: string } }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404":
          description: The provider key is not configured or not enabled.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/auth/oidc/{provider}/callback:
    get:
      tags: [auth]
      operationId: callbackOIDC
      summary: Finish an OpenID Connect flow
      description: |
        Consumes the IdP's redirect, verifies state + nonce, exchanges the
        code for an access_token + id_token, verifies the id_token (signature
        via JWKS, audience, expiry, nonce), and either logs the user in or
        links the identity.
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          schema: { type: string }
        - name: code
          in: query
          required: true
          schema: { type: string }
        - name: state
          in: query
          required: true
          schema: { type: string }
      responses:
        "302":
          description: Redirect to the dashboard or login page.
          headers:
            Location: { schema: { type: string } }
        "400": { $ref: "#/components/responses/BadRequest" }

  # External identity providers (WS-07b): SAML 2.0 service-provider flow.
  # -------------------------------------------------------------------------
  # Three endpoints: the SP metadata (served at a well-known URL so the IdP
  # can register us), the SP-initiated start (mints a signed AuthnRequest +
  # redirects the browser to the IdP), and the Assertion Consumer Service
  # (POST endpoint the IdP posts the signed assertion back to).

  /api/v1/auth/saml/metadata:
    get:
      tags: [auth]
      operationId: metadataSAML
      summary: SAML service-provider metadata
      description: |
        Serves the SP metadata XML the IdP registers Lahijan under. The
        metadata carries the SP's entity ID, the ACS URL (POST binding), and
        the SP's signing certificate so the IdP can verify signed
        AuthnRequests.

        When no SAML provider is configured, returns a 501 with a localised
        "feature disabled" envelope.
      security: []
      responses:
        "200":
          description: SP metadata XML.
          content:
            application/xml:
              schema:
                type: string
        "501":
          description: SAML is not enabled on this server.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/auth/saml/{provider}/start:
    get:
      tags: [auth]
      operationId: startSAML
      summary: Begin a SAML 2.0 service-provider flow
      description: |
        Mints a signed SAML AuthnRequest, sets a signed CSRF state token as a
        short-lived cookie, and 302s the browser to the IdP's SSO endpoint
        (HTTP-Redirect binding).

        When called from an authenticated session (the user is linking a new
        SAML IdP to their existing account) the response also sets a
        lahijan_link_uid cookie carrying the user's id; the ACS enforces the
        link against the user who started the flow.
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          description: The configured SAML provider key (e.g. entra, okta).
          schema: { type: string }
      responses:
        "302":
          description: Redirect to the IdP SSO URL.
          headers:
            Location:
              description: The absolute IdP SSO URL carrying SAMLRequest + RelayState.
              schema: { type: string }
        "400": { $ref: "#/components/responses/BadRequest" }
        "404":
          description: The provider key is not configured or not enabled.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/auth/saml/{provider}/acs:
    post:
      tags: [auth]
      operationId: assertionConsumerServiceSAML
      summary: SAML Assertion Consumer Service
      description: |
        Consumes the IdP's POSTed SAML response (form fields SAMLResponse +
        RelayState), verifies the state token against the cookie nonce,
        verifies the signed assertion (signature via the IdP's published
        certificate, audience, recipient, conditions, InResponseTo replay
        check), and either:
          - logs the user in (sets the session + refresh cookies), or
          - links the SAML identity to the logged-in user (when
            lahijan_link_uid is set), or
          - returns a "completion required" envelope when the (provider,
            name_id) is unknown and just-in-time user creation is disabled.

        On success the browser is redirected to the dashboard root (`/`);
        on failure it is redirected to the login page with an error query.
      security: []
      parameters:
        - name: provider
          in: path
          required: true
          schema: { type: string }
      requestBody:
        required: true
        content:
          application/x-www-form-urlencoded:
            schema:
              type: object
              required: [SAMLResponse]
              properties:
                SAMLResponse:
                  type: string
                  description: Base64-encoded SAML Response XML the IdP signed.
                RelayState:
                  type: string
                  description: The relay state the SP emitted on /start (carries the CSRF token).
      responses:
        "302":
          description: Redirect to the dashboard or login page.
          headers:
            Location: { schema: { type: string } }
        "400": { $ref: "#/components/responses/BadRequest" }

  /api/v1/me/identities:
    get:
      tags: [auth]
      operationId: listMyIdentities
      summary: List the current user's linked external identities
      description: |
        Returns every external identity (OAuth + OIDC) linked to the current
        user. Tokens are NOT included; only the provider key, subject, and
        scopes are returned.
      responses:
        "200":
          description: The caller's linked identities.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ExternalIdentity"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/v1/me/identities/{identityId}:
    delete:
      tags: [auth]
      operationId: deleteMyIdentity
      summary: Unlink an external identity from the current user
      description: |
        Removes the (user, provider) link. Refuses if removing it would leave
        the user with no way to log in (no password and no other identities).
      parameters:
        - name: identityId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Unlinked.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MessageResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: |
            Removing this identity would leave the user with no way to log in.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

  # -------------------------------------------------------------------------
  # Multi-factor authentication (WS-07c)
  # -------------------------------------------------------------------------
  # Self-service factor enrollment lives under /api/v1/me/mfa/*. The login
  # challenge endpoint is under /api/v1/auth/mfa/challenge because the caller
  # is anonymous (only carries the pending_session_token).

  /api/v1/me/mfa/totp/enroll:
    post:
      tags: [auth]
      operationId: enrollTOTP
      summary: Begin TOTP enrollment
      description: |
        Generates a fresh TOTP secret, AES-GCM-encrypts it at rest, and
        returns the raw secret + the otpauth:// provisioning URI for the
        dashboard to render as a QR code. Until the user confirms with a
        valid 6-digit code at /verify, the secret does NOT count as an
        enrolled factor (login does not challenge for it).

        Re-enrollment over a confirmed factor fails with 409 — the user
        must explicitly disable first.
      responses:
        "200":
          description: The new TOTP secret + provisioning URI.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TOTPEnrollResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409":
          description: TOTP is already enrolled; disable first.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/me/mfa/totp/verify:
    post:
      tags: [auth]
      operationId: verifyTOTP
      summary: Confirm TOTP enrollment with a 6-digit code
      description: |
        Marks the pending TOTP secret as confirmed (the user's
        authenticator app produced a matching 6-digit code). On success
        the factor becomes effective and a fresh batch of recovery codes
        is generated (returned once via /me/mfa/recovery).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TOTPVerifyRequest"
      responses:
        "200":
          description: TOTP confirmed; recovery codes generated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryCodesBatch"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/me/mfa/totp/disable:
    post:
      tags: [auth]
      operationId: disableTOTP
      summary: Disable TOTP
      description: |
        Removes the user's TOTP factor. Per the WS-07c default the caller
        MUST supply their current password (re-authentication) before the
        factor is removed; this prevents a stolen session cookie from
        silently disarming MFA.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFADisableRequest"
      responses:
        "200":
          description: TOTP disabled.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessageResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/me/mfa/webauthn/register/begin:
    post:
      tags: [auth]
      operationId: beginWebAuthnRegistration
      summary: Begin WebAuthn / passkey registration
      description: |
        Returns the PublicKeyCredentialCreationOptions the browser feeds
        to navigator.credentials.create(). The dashboard persists the
        returned ceremony session (typically in a short-lived cookie or
        local storage) and POSTs it back at /register/finish.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebAuthnBeginRequest"
      responses:
        "200":
          description: The ceremony options + opaque session handle.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebAuthnBeginRegistrationResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "501":
          description: WebAuthn is not configured on this server.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/me/mfa/webauthn/register/finish:
    post:
      tags: [auth]
      operationId: finishWebAuthnRegistration
      summary: Finish WebAuthn / passkey registration
      description: |
        Accepts the attestation response the browser POSTed after
        navigator.credentials.create() succeeded. Verifies the signature,
        persists the credential, and (on success) returns a small
        confirmation envelope.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebAuthnFinishRegistrationRequest"
      responses:
        "200":
          description: The credential was registered.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessageResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/me/mfa/webauthn/login/begin:
    post:
      tags: [auth]
      operationId: beginWebAuthnLogin
      summary: Begin a WebAuthn login ceremony
      description: |
        Returns the PublicKeyCredentialRequestOptions the browser feeds
        to navigator.credentials.get() during an MFA challenge. The
        caller MUST carry a valid pending_session_token (issued by the
        password/IdP login step when MFA is required).
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebAuthnLoginBeginRequest"
      responses:
        "200":
          description: The ceremony options + opaque session handle.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebAuthnLoginBeginResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/me/mfa/webauthn/login/finish:
    post:
      tags: [auth]
      operationId: finishWebAuthnLogin
      summary: Finish a WebAuthn login ceremony
      description: |
        Accepts the assertion response the browser POSTed after
        navigator.credentials.get() succeeded. On success the MFA
        challenge is complete; the caller still POSTs to
        /api/v1/auth/mfa/challenge with kind=webauthn-finished to
        exchange the pending_session_token for the real session.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WebAuthnLoginFinishRequest"
      responses:
        "200":
          description: The assertion verified; the challenge is complete.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessageResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/me/mfa/webauthn/credentials/{credentialId}:
    delete:
      tags: [auth]
      operationId: deleteWebAuthnCredential
      summary: Remove a WebAuthn credential
      description: |
        Revokes a single WebAuthn credential (one of the user's passkeys).
        Re-authentication (current password) is required when this would
        leave the user with zero factors AND MFA is required by policy.
      parameters:
        - name: credentialId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Credential removed.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessageResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/me/mfa/recovery:
    get:
      tags: [auth]
      operationId: listMyRecoveryCodes
      summary: List recovery code metadata (no raw codes)
      description: |
        Returns metadata about the user's recovery codes (count remaining,
        created_at per row). The raw codes are NEVER returned by this
        endpoint — they were shown exactly once at generation time.
      responses:
        "200":
          description: The user's recovery code rows (raw codes stripped).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryCodesListResponse"
        "401": { $ref: "#/components/responses/Unauthorized" }
    post:
      tags: [auth]
      operationId: regenerateMyRecoveryCodes
      summary: Regenerate the recovery code batch
      description: |
        Wipes the user's existing recovery codes (used + unused) and
        issues a fresh batch. The raw codes are returned exactly once;
        Lahijan cannot show them again.
      responses:
        "200":
          description: The fresh batch of recovery codes (shown once).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RecoveryCodesBatch"
        "401": { $ref: "#/components/responses/Unauthorized" }

  /api/v1/auth/mfa/challenge:
    post:
      tags: [auth]
      operationId: challengeMFA
      summary: Verify an MFA challenge and issue the real session
      description: |
        Accepts a pending_session_token (from /login or /oauth/callback
        when MFA was required) plus an MFA code:

          - kind=totp:     code is a 6-digit TOTP value
          - kind=recovery: code is a recovery code
          - kind=webauthn: assertion fields from the /webauthn/login/finish response

        On success the pending_session_token is consumed and the real
        session + refresh token are issued (set as cookies, just like
        /login). On failure the failed_attempts counter is bumped; at
        maxAttempts=5 the pending token is revoked (brute-force lockout).
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MFAChallengeRequest"
      responses:
        "200":
          description: MFA succeeded; real session cookies set.
          headers:
            Set-Cookie: { $ref: "#/components/headers/SetCookie" }
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuthResponse" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401":
          description: |
            The pending token is invalid, expired, already used, or
            revoked; OR the supplied code did not match.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "429":
          description: Too many failed attempts; pending session revoked.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  # -------------------------------------------------------------------------
  # Audit log (WS-08)
  # -------------------------------------------------------------------------
  # /api/v1/audit returns the tenant-scoped audit trail; the caller must be a
  # member of the tenant (set via X-Tenant-Id) and hold audit.read.
  # /api/v1/audit/global is the superuser variant (audit.read_global).

  /api/v1/audit:
    get:
      tags: [audit]
      operationId: listAudit
      summary: List audit events for the current tenant
      description: |
        Returns the tenant-scoped audit trail, filtered by the optional query
        parameters. The caller must be authenticated, supply a tenant scope
        via the X-Tenant-Id header, and hold the audit.read permission.

        System-level events (e.g. auth login) carry a NULL tenant_id and are
        visible from every tenant so operators can trace user flows end to
        end.
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
        - name: actorUserId
          in: query
          schema: { type: string, format: uuid }
        - name: action
          in: query
          schema: { type: string }
        - name: resourceType
          in: query
          schema: { type: string }
        - name: status
          in: query
          schema: { type: string, enum: [success, failure, pending] }
        - name: actorType
          in: query
          schema: { type: string, enum: [user, system, plugin] }
        - name: fromTs
          in: query
          schema: { type: string, format: date-time }
        - name: toTs
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: A page of audit events.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditPage"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/v1/audit/{auditId}:
    get:
      tags: [audit]
      operationId: getAudit
      summary: Fetch a single audit event by id
      description: |
        Returns the audit row plus its outcome trail (the latest outcome's
        status is the event's current status). System-level events (NULL
        tenant_id) are visible from any tenant.
      parameters:
        - name: auditId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The audit event.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AuditEvent" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/audit/export:
    get:
      tags: [audit]
      operationId: exportAudit
      summary: Export the tenant audit log (CSV or JSON)
      description: |
        Streams the matching audit rows as either CSV (the default) or JSON.
        Same filters as /audit but no pagination — the caller gets every
        matching row, chunked. Requires the audit.export permission; the
        export itself is recorded as an audit event (action: audit.export).
      parameters:
        - $ref: "#/components/parameters/ExportFormat"
        - name: actorUserId
          in: query
          schema: { type: string, format: uuid }
        - name: action
          in: query
          schema: { type: string }
        - name: resourceType
          in: query
          schema: { type: string }
        - name: status
          in: query
          schema: { type: string, enum: [success, failure, pending] }
        - name: actorType
          in: query
          schema: { type: string, enum: [user, system, plugin] }
        - name: fromTs
          in: query
          schema: { type: string, format: date-time }
        - name: toTs
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: The audit rows in the requested format.
          content:
            text/csv:
              schema: { type: string }
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/AuditEvent" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  # -------------------------------------------------------------------------
  # Admin: job system (WS-09). Platform-admin-only endpoints for inspecting
  # and controlling River's durable job queue. Every privileged action also
  # emits an audit event (action: platform.job.retry | platform.job.cancel).
  # River's built-in web UI is mounted at /admin/jobs/ui (NOT under /api/v1/
  # because it ships its own REST API); the same platform.jobs.read
  # permission gates both.
  # -------------------------------------------------------------------------

  /api/v1/admin/jobs:
    get:
      tags: [admin]
      operationId: listAdminJobs
      summary: List queued / running / failed jobs
      description: |
        Returns a filtered, paginated page of jobs from the durable job
        queue (River). The caller MUST hold the platform.jobs.read
        permission (i.e. be a platform.admin). Cross-tenant by design:
        jobs are global; the platform admin can see every tenant's work.
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
        - name: state
          in: query
          description: |
            Filter by job state. When omitted, every state is returned.
            Multiple values are OR'd together.
          schema:
            type: array
            items:
              type: string
              enum: [available, pending, retryable, running, scheduled, completed, cancelled, discarded]
        - name: kind
          in: query
          description: Filter by exact job kind (e.g. "billing.usage.rollup").
          schema:
            type: string
        - name: queue
          in: query
          description: Filter by queue name.
          schema:
            type: string
      responses:
        "200":
          description: A page of jobs.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AdminJobPage"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/jobs/{jobId}:
    get:
      tags: [admin]
      operationId: getAdminJob
      summary: Fetch a single job by id
      description: |
        Returns the full job row (state, args, attempt history, errors) for
        the given id. Requires platform.jobs.read.
      parameters:
        - name: jobId
          in: path
          required: true
          description: The River job id (a bigserial, not a UUID).
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: The job row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/jobs/{jobId}/retry:
    post:
      tags: [admin]
      operationId: retryAdminJob
      summary: Manually retry a discarded (DLQ'd) job
      description: |
        Re-queues a discarded job for an immediate retry. Requires the
        platform.jobs.retry permission. The action is recorded as an audit
        event (action: platform.job.retry).
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: The job was re-queued; the updated row is returned.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: The job is not in a retryable state (e.g. still running).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/admin/jobs/{jobId}/cancel:
    post:
      tags: [admin]
      operationId: cancelAdminJob
      summary: Cancel a queued or running job
      description: |
        Marks the job as cancelled. Already-running jobs get a cancellation
        signal via context. Requires the platform.jobs.cancel permission.
        The action is recorded as an audit event (action:
        platform.job.cancel).
      parameters:
        - name: jobId
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        "200":
          description: The job was cancelled; the updated row is returned.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminJob" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: The job is already in a terminal state.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  # -------------------------------------------------------------------------
  # Admin: plugin system (WS-10a). Platform-admin-only endpoints for
  # uploading, listing, inspecting, granting, enabling, disabling, and
  # deleting WASM plugins. Every state-changing action emits an audit event
  # (action: plugins.upload | plugins.grant | plugins.revoke | plugins.enable
  # | plugins.disable | plugins.delete). The runtime itself (instantiation)
  # is gated behind conf.wasm.enabled; when the runtime is disabled these
  # endpoints still work (the plugin row is persisted in "pending") and the
  # runtime will compile on first instantiation after the operator turns
  # wasm.enabled on.
  # -------------------------------------------------------------------------

  /api/v1/admin/plugins:
    get:
      tags: [admin]
      operationId: listAdminPlugins
      summary: List installed plugins
      description: |
        Returns a paginated list of plugins visible to the caller. A
        platform.admin sees every plugin across every tenant; a tenant
        admin sees their tenant's plugins plus every platform-wide plugin
        (tenant_id IS NULL).
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of plugins.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminPluginPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/admin/plugins/upload:
    post:
      tags: [admin]
      operationId: uploadAdminPlugin
      summary: Upload a new plugin (.wasm + manifest)
      description: |
        Accepts multipart/form-data with two parts:

          * `wasm`     — the compiled .wasm bytes (binary).
          * `manifest` — the lahijan.manifest.yaml text.

        The server parses + validates the manifest, compiles the wasm bytes
        under the configured memory cap (rejecting modules that declare more
        memory than `wasm.max_memory_per_plugin`), persists a new plugin row
        in "pending" status, and emits an audit event. The plugin is NOT
        active after upload; the admin must grant at least one permission
        (or call /enable directly when no permissions are needed) before
        the runtime will instantiate it.
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [wasm, manifest]
              properties:
                wasm:
                  type: string
                  format: binary
                manifest:
                  type: string
      responses:
        "201":
          description: The plugin was uploaded; the new row is returned.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminPlugin" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: A plugin with this (name, version) already exists.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "413":
          description: The .wasm module exceeds the configured max upload size.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/admin/plugins/{pluginId}:
    get:
      tags: [admin]
      operationId: getAdminPlugin
      summary: Fetch a single plugin by id
      description: |
        Returns the full plugin row including the parsed manifest and the
        list of granted permissions. The wasm bytes are NOT included (they
        can be very large); a separate download endpoint will land in a
        follow-up WS if needed.
      parameters:
        - name: pluginId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The plugin row + grants.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminPlugin" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [admin]
      operationId: deleteAdminPlugin
      summary: Delete a plugin
      description: |
        Hard-deletes the plugin row and its grants (CASCADE). Active
        instances are cancelled at the next host-call boundary. Emits an
        audit event (action: plugins.delete).
      parameters:
        - name: pluginId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Deleted.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/MessageResponse" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/plugins/{pluginId}/permissions/{permission}/{action}:
    post:
      tags: [admin]
      operationId: setAdminPluginPermission
      summary: Grant or revoke a single permission on a plugin
      description: |
        {action} is one of `grant` | `revoke`. The {permission} path
        segment is the slug (e.g. `kv.read:cache`); URL-encode the `:`
        separator as `%3A` if your client does not do so automatically.
        Grants are idempotent; revokes are no-ops when the slug is not
        held. Every call emits an audit event (action: plugins.grant |
        plugins.revoke) carrying the permission slug in metadata.
      parameters:
        - name: pluginId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: permission
          in: path
          required: true
          schema: { type: string }
          description: The permission slug (e.g. "kv.read:cache").
        - name: action
          in: path
          required: true
          schema: { type: string, enum: [grant, revoke] }
      responses:
        "200":
          description: The grant/revoke was applied; the updated grant list is returned.
          content:
            application/json:
              schema:
                type: array
                items: { type: string }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/plugins/{pluginId}/enable:
    post:
      tags: [admin]
      operationId: enableAdminPlugin
      summary: Enable a plugin
      description: |
        Flips the plugin's status from "pending" or "disabled" to "active".
        The runtime may now instantiate it on demand. Emits an audit event
        (action: plugins.enable).
      parameters:
        - name: pluginId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The plugin was enabled; the updated row is returned.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminPlugin" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/admin/plugins/{pluginId}/disable:
    post:
      tags: [admin]
      operationId: disableAdminPlugin
      summary: Disable a plugin
      description: |
        Flips the plugin's status to "disabled". Grants persist; calling
        /enable picks them back up. Emits an audit event (action:
        plugins.disable).
      parameters:
        - name: pluginId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The plugin was disabled; the updated row is returned.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminPlugin" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # -------------------------------------------------------------------------
  # Admin: plugin marketplace (WS-10c). Platform-admin-only endpoints for
  # browsing the configured marketplace index and installing or upgrading
  # plugins from it. Every state-changing action emits an audit event
  # (action: plugins.marketplace_install | plugins.marketplace_upgrade |
  # plugins.upgrade). The marketplace source is configured via
  # wasm.marketplace.url (HTTP) or wasm.marketplace.path (local directory).
  # -------------------------------------------------------------------------

  /api/v1/admin/marketplace:
    get:
      tags: [admin]
      operationId: listAdminMarketplace
      summary: List plugins available in the marketplace
      description: |
        Returns the configured marketplace index verbatim. Each entry
        carries the plugin's marketplace metadata (name, version,
        description, license, permissions) plus the sha256 pin the
        installer verifies before persisting. The marketplace itself
        is configured via conf.wasm.marketplace.{url,path}.
      responses:
        "200":
          description: A page of marketplace entries.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminMarketplacePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501":
          description: The marketplace subsystem is not enabled on this server.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/admin/marketplace/{name}:
    get:
      tags: [admin]
      operationId: getAdminMarketplaceEntry
      summary: Fetch a single marketplace entry by name
      description: |
        Returns one entry from the marketplace index. Useful for the
        admin UI's "install" page which renders the manifest + permission
        list before the admin approves.
      parameters:
        - name: name
          in: path
          required: true
          schema: { type: string }
          description: The marketplace entry name (kebab-case).
      responses:
        "200":
          description: The entry.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminMarketplaceEntry" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501":
          description: The marketplace subsystem is not enabled on this server.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/admin/plugins/install/{name}:
    post:
      tags: [admin]
      operationId: installAdminPluginFromMarketplace
      summary: Install a plugin from the marketplace
      description: |
        Fetches the plugin from the configured marketplace (verifying
        the sha256 pin), runs it through the standard installer.Upload
        flow (audit emission, permission enforcement), and returns the
        new plugin row in "pending" status. The admin must grant at
        least one permission (or call /enable directly) before the
        runtime will instantiate it.

        Returns 409 Conflict when a plugin with this (name, *) already
        exists; the admin should POST /upgrade/{name} instead.
      parameters:
        - name: name
          in: path
          required: true
          schema: { type: string }
          description: The marketplace entry name (kebab-case).
      responses:
        "201":
          description: The plugin was installed; the new row is returned.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminPlugin" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: The marketplace has no entry with this name.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: A plugin with this name is already installed (use /upgrade instead).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "422":
          description: The downloaded .wasm does not match the marketplace sha256 pin.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "501":
          description: The marketplace subsystem is not enabled on this server.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/admin/plugins/upgrade/{name}:
    post:
      tags: [admin]
      operationId: upgradeAdminPluginFromMarketplace
      summary: Upgrade an installed plugin to the marketplace's latest version
      description: |
        Fetches the marketplace version of an already-installed plugin
        and runs it through installer.Upgrade. The flow:

          1. Locates the most recent prior version (by name).
          2. Semver-compares; rejects downgrades + same-version
             reinstalls.
          3. Uploads the new version (status=pending), preserving
             grants the new manifest still requests, dropping grants
             it no longer requests, and surfacing new permissions in
             the response so the admin can approve them.
          4. Hard-deletes the old row. CASCADE removes orphan state.

        The response includes `newPermissions` — the admin must POST
        /permissions/{perm}/grant for each before /enable will work.
      parameters:
        - name: name
          in: path
          required: true
          schema: { type: string }
          description: The marketplace entry name (kebab-case).
      responses:
        "200":
          description: The upgrade completed; the result bundle is returned.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/AdminPluginUpgradeResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404":
          description: The marketplace has no entry with this name OR no prior installation exists.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "409":
          description: The marketplace version equals the installed version.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "422":
          description: The downloaded .wasm does not match the marketplace sha256 pin.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "501":
          description: The marketplace subsystem is not enabled on this server.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  # ===========================================================================
  # Compute module (WS-14)
  # ===========================================================================

  /api/v1/compute/instances:
    get:
      tags: [compute]
      operationId: listComputeInstances
      summary: List compute instances
      description: |
        Returns a paginated list of the caller's compute instances within
        the tenant in scope. The list reflects the cached state in the
        Lahijan database; live state from the daemon is queried on demand
        via GET /instances/{id} (which reconciles) or via the per-instance
        reconcile endpoint (TODO).
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of instances.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeInstancePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [compute]
      operationId: createComputeInstance
      summary: Create a compute instance
      description: |
        Creates a new instance in the caller's tenant. The service checks
        the per-tenant quota (vCPU / RAM / disk / instance count) before
        reaching the daemon; an over-quota request returns 422. The
        instance is created in "stopped" state; the caller must POST
        /instances/{id}/start to launch it.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeInstanceCreateRequest" }
      responses:
        "201":
          description: Instance created (in stopped state).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeInstance" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "402":
          description: Insufficient balance to create the instance (per ADR-0013).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }
        "422":
          description: Quota exceeded.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "501":
          description: The compute subsystem is not enabled on this server.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/compute/instances/{instanceId}:
    get:
      tags: [compute]
      operationId: getComputeInstance
      summary: Get a compute instance (reconciled)
      description: |
        Returns the instance, reconciling the cached row with the live
        daemon state. A flaky daemon returns the cached state instead of
        erroring so the UI does not 500 on a backend blip.
      parameters:
        - name: instanceId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The instance.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeInstance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      tags: [compute]
      operationId: updateComputeInstance
      summary: Update an instance's config
      description: |
        Replaces the instance's description, config, devices, and profiles.
        Hot-pluggable fields (e.g. limits.memory for a stopped instance)
        apply immediately; others require a restart.
      parameters:
        - name: instanceId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeInstanceUpdateRequest" }
      responses:
        "200":
          description: The updated instance.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeInstance" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeInstance
      summary: Delete a compute instance
      description: |
        Stops the instance if running (force = true skips the graceful
        shutdown), deletes it from the daemon, and soft-deletes the
        cached row (the row is retained for historical audit + billing
        joins).
      parameters:
        - name: instanceId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: force
          in: query
          required: false
          schema: { type: boolean, default: false }
          description: Skip the graceful shutdown and force-delete.
      responses:
        "204": { description: Instance deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/compute/instances/{instanceId}/{action}:
    parameters:
      - name: instanceId
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: action
        in: path
        required: true
        schema: { type: string, enum: [start, stop, restart, freeze, unfreeze] }
      - name: force
        in: query
        required: false
        schema: { type: boolean, default: false }
        description: Force the action (skip graceful shutdown for stop).
      - name: timeout
        in: query
        required: false
        schema: { type: integer, minimum: 0, default: 30 }
        description: Action timeout in seconds.
    post:
      tags: [compute]
      operationId: setComputeInstanceState
      summary: Apply a lifecycle action to an instance
      description: |
        Starts, stops, restarts, freezes, or unfreezes an instance. The
        cached row is updated with the new status; the audit log records
        the action and the WASM event bus emits the corresponding
        compute.instance.* event.
      responses:
        "200":
          description: The updated instance.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeInstance" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: The action conflicts with the current state (e.g. start on a running instance).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/compute/instances/{instanceId}/exec:
    post:
      tags: [compute]
      operationId: execComputeInstance
      summary: Run a one-shot command inside an instance
      description: |
        Runs the given command in the instance and returns the captured
        stdout, stderr, and exit code. The interactive (bidirectional,
        xterm.js) variant lands with WS-20 (dashboard UI); this endpoint
        is the simpler "run + capture" path the API exposes today. The
        instance must be running (WS-14 open question 3).
      parameters:
        - name: instanceId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeExecRequest" }
      responses:
        "200":
          description: The captured output.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeExecResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: The instance is not running.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  # WS-24: noVNC graphical console. WebSocket-only — oapi-codegen can't
  # model the WS upgrade so the handler is hand-written; the operationId
  # keeps the generated method name reviewable. The audit gate dispatches
  # this path to RequirePerm(compute.instance.console.vnc).
  /api/v1/compute/instances/{instanceId}/vnc:
    parameters:
      - name: instanceId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: openVncComputeInstance
      summary: Open a graphical (noVNC) console session to a VM instance (WebSocket)
      description: |
        Upgrades the HTTP request to a WebSocket and bridges the browser's
        noVNC client to the Incus VM VGA console for the given instance.
        The instance MUST be a virtual machine and MUST be running. The
        privileged action (session-open) is gated by
        compute.instance.console.vnc and emits an audit row with action
        compute.instance.console.vnc.connect before the bytes flow. The
        WebSocket carries raw RFB (VNC) bytes both directions until either
        side closes. Same-origin session-cookie auth; the browser sends
        cookies on the WS upgrade automatically.

        Note: although this endpoint has an operationId (so the generated
        ServerInterface method name is reviewable), oapi-codegen cannot
        model the WebSocket upgrade itself — the handler is hand-written
        in internal/app/lahijan/api/compute_vnc_handlers.go.
      responses:
        "101":
          description: Switching Protocols — WebSocket upgrade succeeded.
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: The instance is not a virtual machine, or is not running.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "503":
          description: The graphical console could not be opened (e.g. daemon down, VM agent not ready).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }

  /api/v1/compute/images:
    get:
      tags: [compute]
      operationId: listComputeImages
      summary: List compute images
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of images.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeImagePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [compute]
      operationId: uploadComputeImage
      summary: Record a custom image
      description: |
        Records a custom image row in the tenant's catalog. The actual
        upload to the image store is performed by the provider driver;
        this endpoint persists the alias + fingerprint so the catalog is
        listable and the alias resolves at instance-create time.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeImageUploadRequest" }
      responses:
        "201":
          description: Image recorded.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeImage" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/compute/images/{imageId}:
    parameters:
      - name: imageId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: getComputeImage
      summary: Get an image
      responses:
        "200":
          description: The image.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeImage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeImage
      summary: Delete a custom image
      description: Featured images cannot be deleted.
      responses:
        "204": { description: Image deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/compute/profiles:
    get:
      tags: [compute]
      operationId: listComputeProfiles
      summary: List profiles
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of profiles.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeProfilePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [compute]
      operationId: createComputeProfile
      summary: Create a profile
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeProfileCreateRequest" }
      responses:
        "201":
          description: Profile created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeProfile" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/compute/profiles/{profileId}:
    parameters:
      - name: profileId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: getComputeProfile
      summary: Get a profile
      responses:
        "200":
          description: The profile.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeProfile" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeProfile
      summary: Delete a profile
      responses:
        "204": { description: Profile deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/compute/networks:
    get:
      tags: [compute]
      operationId: listComputeNetworks
      summary: List networks
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of networks.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeNetworkPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [compute]
      operationId: createComputeNetwork
      summary: Create a network
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeNetworkCreateRequest" }
      responses:
        "201":
          description: Network created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeNetwork" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/compute/networks/{networkId}:
    parameters:
      - name: networkId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: getComputeNetwork
      summary: Get a network
      responses:
        "200":
          description: The network.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeNetwork" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeNetwork
      summary: Delete a network
      responses:
        "204": { description: Network deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/compute/storage:
    get:
      tags: [compute]
      operationId: listComputeStorageVolumes
      summary: List storage volumes
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of storage volumes.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeStorageVolumePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [compute]
      operationId: createComputeStorageVolume
      summary: Create a storage volume
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeStorageVolumeCreateRequest" }
      responses:
        "201":
          description: Volume created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeStorageVolume" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/compute/storage/{volumeId}:
    parameters:
      - name: volumeId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: getComputeStorageVolume
      summary: Get a storage volume
      responses:
        "200":
          description: The volume.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeStorageVolume" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeStorageVolume
      summary: Delete a storage volume
      responses:
        "204": { description: Volume deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ===========================================================================
  # Compute snapshots + backups + policies (WS-25)
  # ===========================================================================

  /api/v1/compute/instances/{instanceId}/snapshots:
    parameters:
      - name: instanceId
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: limit
        in: query
        schema: { type: integer, minimum: 1, maximum: 200 }
      - name: offset
        in: query
        schema: { type: integer, minimum: 0 }
    get:
      tags: [compute]
      operationId: listComputeSnapshots
      summary: List snapshots of an instance
      responses:
        "200":
          description: A page of snapshots.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeSnapshotPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      tags: [compute]
      operationId: createComputeSnapshot
      summary: Take a snapshot of an instance
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeSnapshotCreateRequest" }
      responses:
        "201":
          description: The snapshot row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeSnapshot" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/compute/instances/{instanceId}/snapshots/{snapshotId}:
    parameters:
      - name: instanceId
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: snapshotId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: getComputeSnapshot
      summary: Get a snapshot
      responses:
        "200":
          description: The snapshot.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeSnapshot" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeSnapshot
      summary: Delete a snapshot
      responses:
        "204": { description: Snapshot deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/compute/instances/{instanceId}/snapshots/{snapshotId}/restore:
    parameters:
      - name: instanceId
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: snapshotId
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [compute]
      operationId: restoreComputeSnapshot
      summary: Restore the instance to a snapshot
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeSnapshotRestoreRequest" }
      responses:
        "200":
          description: The restored snapshot row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeSnapshot" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/compute/instances/{instanceId}/backups:
    parameters:
      - name: instanceId
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: limit
        in: query
        schema: { type: integer, minimum: 1, maximum: 200 }
      - name: offset
        in: query
        schema: { type: integer, minimum: 0 }
    get:
      tags: [compute]
      operationId: listComputeBackupsByInstance
      summary: List off-host backups for an instance
      responses:
        "200":
          description: A page of backups.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeBackupPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/compute/snapshot-policies:
    parameters:
      - name: limit
        in: query
        schema: { type: integer, minimum: 1, maximum: 200 }
      - name: offset
        in: query
        schema: { type: integer, minimum: 0 }
    get:
      tags: [compute]
      operationId: listComputeSnapshotPolicies
      summary: List snapshot schedule policies
      responses:
        "200":
          description: A page of policies.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeSnapshotPolicyPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [compute]
      operationId: createComputeSnapshotPolicy
      summary: Create a snapshot schedule policy
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeSnapshotPolicyCreateRequest" }
      responses:
        "201":
          description: The policy row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeSnapshotPolicy" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/compute/snapshot-policies/{policyId}:
    parameters:
      - name: policyId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: getComputeSnapshotPolicy
      summary: Get a snapshot schedule policy
      responses:
        "200":
          description: The policy.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeSnapshotPolicy" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    patch:
      tags: [compute]
      operationId: updateComputeSnapshotPolicy
      summary: Update a snapshot schedule policy
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeSnapshotPolicyUpdateRequest" }
      responses:
        "200":
          description: The updated policy.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeSnapshotPolicy" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeSnapshotPolicy
      summary: Delete a snapshot schedule policy
      responses:
        "204": { description: Policy deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/compute/backup-targets:
    parameters:
      - name: limit
        in: query
        schema: { type: integer, minimum: 1, maximum: 200 }
      - name: offset
        in: query
        schema: { type: integer, minimum: 0 }
    get:
      tags: [compute]
      operationId: listComputeBackupTargets
      summary: List off-host backup targets
      responses:
        "200":
          description: A page of targets.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeBackupTargetPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      tags: [compute]
      operationId: createComputeBackupTarget
      summary: Create an off-host backup target
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ComputeBackupTargetCreateRequest" }
      responses:
        "201":
          description: The target row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeBackupTarget" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /api/v1/compute/backup-targets/{targetId}:
    parameters:
      - name: targetId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: getComputeBackupTarget
      summary: Get a backup target
      responses:
        "200":
          description: The target.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeBackupTarget" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeBackupTarget
      summary: Delete a backup target
      responses:
        "204": { description: Target deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /api/v1/compute/backups:
    parameters:
      - name: limit
        in: query
        schema: { type: integer, minimum: 1, maximum: 200 }
      - name: offset
        in: query
        schema: { type: integer, minimum: 0 }
    get:
      tags: [compute]
      operationId: listComputeBackups
      summary: List off-host backups
      responses:
        "200":
          description: A page of backups.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeBackupPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/compute/backups/{backupId}:
    parameters:
      - name: backupId
        in: path
        required: true
        schema: { type: string, format: uuid }
    get:
      tags: [compute]
      operationId: getComputeBackup
      summary: Get a backup
      responses:
        "200":
          description: The backup.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ComputeBackup" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    delete:
      tags: [compute]
      operationId: deleteComputeBackup
      summary: Delete a backup (removes remote bytes + soft-deletes the row)
      responses:
        "204": { description: Backup deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  # ===========================================================================
  # DNS module (WS-15)
  # ===========================================================================

  /api/v1/dns/zones:
    get:
      tags: [dns]
      operationId: listDNSZones
      summary: List DNS zones
      description: |
        Returns a paginated list of the caller's DNS zones within the tenant
        in scope. The list reflects the cached state in the Lahijan DB; a
        per-zone Reconcile call refreshes the DNSSEC flag from PDNS on
        demand.
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of zones.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DNSZonePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    post:
      tags: [dns]
      operationId: createDNSZone
      summary: Create a DNS zone
      description: |
        Creates a new zone in the caller's tenant. The canonical name must
        be globally unique (per WS-15 Open Question 3: per-tenant
        uniqueness at the API, but two tenants cannot own the same
        canonical id because the canonical_id column is globally unique).
        The PowerDNS daemon is asked to bootstrap the zone with the
        configured default nameservers; DNSSEC is off by default per
        WS-12 Open Question 1.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DNSZoneCreateRequest" }
      responses:
        "201":
          description: Zone created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DNSZone" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: Zone already exists (canonical id claimed by another tenant).
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/dns/zones/{zoneId}:
    get:
      tags: [dns]
      operationId: getDNSZone
      summary: Get a DNS zone (reconciled)
      description: |
        Returns the zone, reconciling the cached DNSSEC flag with the live
        PDNS state. A flaky daemon returns the cached state instead of
        erroring so the UI does not 500 on a backend blip.
      parameters:
        - name: zoneId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The zone.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DNSZone" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    patch:
      tags: [dns]
      operationId: updateDNSZone
      summary: Update a DNS zone's settings
      description: |
        Replaces the zone's description and/or kind. The kind change also
        propagates to PowerDNS so the daemon's view matches.
      parameters:
        - name: zoneId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DNSZoneUpdateRequest" }
      responses:
        "200":
          description: The updated zone.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DNSZone" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    delete:
      tags: [dns]
      operationId: deleteDNSZone
      summary: Delete a DNS zone
      description: |
        Removes the zone from PowerDNS, drops every owned dns_records row,
        and removes the dns_zones row. Idempotent on the daemon side: if
        PDNS reports 404 we still proceed so the dns_zones row is removed.
      parameters:
        - name: zoneId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Zone deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/dns/zones/{zoneId}/records:
    get:
      tags: [dns]
      operationId: listDNSRecords
      summary: List DNS records in a zone
      description: |
        Returns a paginated list of the records in the zone. The cache is
        returned; reconciliation is implicit on every read because the
        dns_records table is the source of truth on the Lahijan side.
      parameters:
        - name: zoneId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of records.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DNSRecordPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    post:
      tags: [dns]
      operationId: createDNSRecord
      summary: Create a DNS record
      description: |
        Creates a new record in the zone. The content is validated per
        type before being submitted to PowerDNS; a malformed content
        returns 400 with a per-type message. CNAME at the zone apex is
        rejected per WS-15 Open Question 2 (strict RFC).
      parameters:
        - name: zoneId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DNSRecordCreateRequest" }
      responses:
        "201":
          description: Record created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DNSRecord" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409":
          description: A record with the same (name, type, content) already exists.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/dns/zones/{zoneId}/records/{recordId}:
    get:
      tags: [dns]
      operationId: getDNSRecord
      summary: Get a DNS record
      parameters:
        - name: zoneId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: recordId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The record.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DNSRecord" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    patch:
      tags: [dns]
      operationId: updateDNSRecord
      summary: Update a DNS record
      description: |
        Replaces the record's content / TTL / disabled flag. The name and
        type are immutable — callers wanting a rename issue a delete +
        create so the audit trail stays honest.
      parameters:
        - name: zoneId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: recordId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DNSRecordUpdateRequest" }
      responses:
        "200":
          description: The updated record.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/DNSRecord" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    delete:
      tags: [dns]
      operationId: deleteDNSRecord
      summary: Delete a DNS record
      parameters:
        - name: zoneId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: recordId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Record deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/dns/zones/{zoneId}/dnssec/{action}:
    parameters:
      - name: zoneId
        in: path
        required: true
        schema: { type: string, format: uuid }
      - name: action
        in: path
        required: true
        schema: { type: string, enum: [enable, disable] }
    post:
      tags: [dns]
      operationId: setDNSZoneDNSSEC
      summary: Enable or disable DNSSEC for a zone
      description: |
        Turns DNSSEC on or off for the zone. Enable creates a combined
        signing key (CSK) via PowerDNS; disable removes every cryptokey.
        Both are idempotent: a double-click is safe.
      responses:
        "204": { description: DNSSEC toggle applied. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/dns/templates:
    get:
      tags: [dns]
      operationId: listDNSTemplates
      summary: List built-in zone templates
      description: |
        Returns the read-only catalog of predefined zone templates
        (Google Workspace setup, Microsoft 365 setup, Google Search
        Console verification, ...). Templates are pure data; apply via
        POST /zones/{id}/apply-template.
      responses:
        "200":
          description: The template catalog.
          content:
            application/json:
              schema:
                type: object
                required: [items]
                additionalProperties: false
                properties:
                  items:
                    type: array
                    items: { $ref: "#/components/schemas/DNSTemplate" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /api/v1/dns/zones/{zoneId}/apply-template:
    parameters:
      - name: zoneId
        in: path
        required: true
        schema: { type: string, format: uuid }
    post:
      tags: [dns]
      operationId: applyDNSTemplate
      summary: Apply a predefined template to a zone
      description: |
        Upserts every record the template defines into the zone. Idempotent:
        running the same template twice updates the records (REPLACE
        semantics) rather than duplicating them.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ApplyDNSTemplateRequest" }
      responses:
        "200":
          description: Template applied. Returns the count of records upserted.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApplyDNSTemplateResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  # =========================================================================
  # Object Storage (WS-16). Per ADR-0011 the data plane is direct from the
  # user's S3 client to the backend; Lahijan only manages the control plane
  # (bucket CRUD, credential mint, presign URL generation, quota config).
  # Every endpoint is tenant-scoped via the X-Tenant-Id header.
  # =========================================================================

  /api/v1/storage/buckets:
    get:
      tags: [storage]
      operationId: listStorageBuckets
      summary: List object storage buckets
      description: |
        Returns a paginated list of the caller's buckets within the tenant
        in scope. The list reflects the cached state in the Lahijan DB;
        per-bucket usage is refreshed by the WS-17 metering job.
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of buckets.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StorageBucketPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    post:
      tags: [storage]
      operationId: createStorageBucket
      summary: Create an object storage bucket
      description: |
        Creates a new bucket in the caller's tenant. The slug must be
        1-26 lowercase alphanumeric characters or dashes per ADR-0011;
        the canonical name "<tenant-uuid>-<slug>" is globally unique.
        The bucket is created on the backend (SeaweedFS) and a row is
        inserted into storage_buckets.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StorageBucketCreateRequest" }
      responses:
        "201":
          description: Bucket created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StorageBucket" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409":
          description: Bucket name (canonical form) is already claimed by another tenant.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/Error" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/storage/buckets/{bucketId}:
    get:
      tags: [storage]
      operationId: getStorageBucket
      summary: Get an object storage bucket
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The bucket.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StorageBucket" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    patch:
      tags: [storage]
      operationId: updateStorageBucket
      summary: Update a bucket's label or description
      description: |
        Replaces the cached label and/or description for the bucket.
        Quota changes go through POST /quota (separate privileged
        action).
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StorageBucketUpdateRequest" }
      responses:
        "200":
          description: The updated bucket.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StorageBucket" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    delete:
      tags: [storage]
      operationId: deleteStorageBucket
      summary: Delete an object storage bucket
      description: |
        Removes the bucket from the backend, revokes every credential
        scoped to it, and soft-deletes the storage_buckets row (the
        row stays for the audit trail). Idempotent on the backend
        side: if the daemon reports 404 we still proceed so the row is
        removed.
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Bucket deleted. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/storage/buckets/{bucketId}/credentials:
    get:
      tags: [storage]
      operationId: listStorageCredentials
      summary: List S3 credentials for a bucket
      description: |
        Returns a paginated list of the non-revoked credentials scoped
        to the bucket. The plaintext secret is NEVER returned; it is
        shown exactly once at mint time.
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of credentials.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StorageCredentialPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    post:
      tags: [storage]
      operationId: createStorageCredential
      summary: Mint an S3 credential for a bucket
      description: |
        Mints a new per-user S3 credential scoped to the bucket. The
        plaintext secret is returned in the response body EXACTLY ONCE;
        subsequent reads return only the access key. Store the secret
        securely — Lahijan cannot recover it.
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StorageCredentialCreateRequest" }
      responses:
        "201":
          description: Credential minted. The plaintext secret is in the response.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StorageCredentialWithSecret" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/storage/buckets/{bucketId}/credentials/{credentialId}:
    delete:
      tags: [storage]
      operationId: revokeStorageCredential
      summary: Revoke an S3 credential
      description: |
        Revokes the credential on the backend and marks the
        storage_credentials row revoked. The access key stops signing
        requests immediately. Idempotent.
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: credentialId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Credential revoked. }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/storage/buckets/{bucketId}/presign:
    post:
      tags: [storage]
      operationId: presignStorageObject
      summary: Generate a pre-signed URL for an object
      description: |
        Returns a SigV4-signed URL the caller can open directly against
        the backend for a limited time. GET URLs are for download; PUT
        URLs are for upload. Per ADR-0011 the data plane is direct —
        the bytes do NOT flow through Lahijan.
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StoragePresignRequest" }
      responses:
        "200":
          description: The pre-signed URL.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StoragePresignResult" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/storage/buckets/{bucketId}/quota:
    post:
      tags: [storage]
      operationId: setStorageBucketQuota
      summary: Set per-bucket quota
      description: |
        Pushes the new quota dimensions to the backend so the daemon
        enforces them on every subsequent PUT. A zero value on either
        dimension clears that ceiling.
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StorageQuotaRequest" }
      responses:
        "204": { description: Quota set. }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/storage/buckets/{bucketId}/usage:
    get:
      tags: [storage]
      operationId: getStorageBucketUsage
      summary: Get cached bucket usage
      description: |
        Returns the cached per-bucket usage (size + object count) plus
        the configured quota. The cache is refreshed by the WS-17
        metering job; a future WS may add a `?live=true` query param
        that triggers a reconcile-on-read.
      parameters:
        - name: bucketId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The cached usage.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StorageBucketUsage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  # -------------------------------------------------------------------------
  # Billing & metering (WS-17). User-facing endpoints under /api/v1/me/*;
  # admin endpoints under /api/v1/admin/billing/* and
  # /api/v1/admin/users/{userId}/*. All amounts are integer centimals.
  # -------------------------------------------------------------------------

  /api/v1/me/balance:
    get:
      tags: [billing]
      operationId: getMyBalance
      summary: Get the caller's cached balance
      description: |
        Returns the cached per-user balance within the caller's tenant.
        The cache is refreshed within 60s of any ledger change; the
        ledger is the source of truth. A user with no ledger history
        returns balance 0.
      responses:
        "200":
          description: The cached balance.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/me/usage:
    get:
      tags: [billing]
      operationId: listMyUsage
      summary: List the caller's usage events
      description: |
        Returns a paginated list of the caller's usage events within the
        tenant in scope. Filterable by resource type and date range.
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
        - name: resourceType
          in: query
          description: Filter by resource type (compute.cpu, storage.size, ...).
          schema: { type: string }
        - name: from
          in: query
          description: Inclusive lower bound on started_at (RFC 3339).
          schema: { type: string, format: date-time }
        - name: to
          in: query
          description: Inclusive upper bound on started_at (RFC 3339).
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: A page of usage events.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingUsagePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/me/ledger:
    get:
      tags: [billing]
      operationId: listMyLedger
      summary: List the caller's ledger entries
      description: |
        Returns a paginated list of the caller's ledger entries within
        the tenant in scope, newest first. The ledger is append-only.
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of ledger entries.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingLedgerPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/me/receipts:
    get:
      tags: [billing]
      operationId: listMyReceipts
      summary: List the caller's receipts
      description: |
        Returns a paginated list of the caller's receipts within the
        tenant in scope, newest period first.
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of receipts.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingReceiptPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    post:
      tags: [billing]
      operationId: generateMyReceipt
      summary: Generate a receipt for a period
      description: |
        Generates (or refreshes) a receipt for the caller covering the
        supplied [periodStart, periodEnd] window. Idempotent: a re-run
        overwrites the PDF body.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BillingReceiptGenerateRequest" }
      responses:
        "201":
          description: The generated receipt.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingReceipt" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/me/receipts/{receiptId}:
    get:
      tags: [billing]
      operationId: getMyReceipt
      summary: Get a receipt's metadata
      description: |
        Returns the receipt's metadata (period, total, currency). The
        PDF body is NOT included; use GET /receipts/{receiptId}.pdf for
        the binary.
      parameters:
        - name: receiptId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The receipt metadata.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingReceipt" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/me/receipts/{receiptId}.pdf:
    get:
      tags: [billing]
      operationId: getMyReceiptPDF
      summary: Download a receipt as PDF
      description: |
        Returns the receipt's PDF body (application/pdf). The body is
        generated by the receipt job or by an on-demand POST to
        /receipts. Stored in-row in the receipts table for MVP.
      parameters:
        - name: receiptId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The PDF body.
          content:
            application/pdf:
              schema: { type: string, format: binary }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/admin/billing/prices:
    get:
      tags: [billing, admin]
      operationId: listAdminBillingPrices
      summary: List the price catalog
      description: |
        Returns a paginated list of the tenant's price catalog entries.
        Requires billing.price_catalog.read.
      parameters:
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of price catalog rows.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingPricePage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    post:
      tags: [billing, admin]
      operationId: upsertAdminBillingPrice
      summary: Upsert (set-current) a price
      description: |
        Atomically closes the currently-in-effect price for (resource,
        unit) and inserts a new one starting at effectiveFrom. Requires
        billing.price_catalog.update. Emits an audit event before +
        after the swap.
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BillingPriceUpsertRequest" }
      responses:
        "201":
          description: The new price row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingPrice" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/admin/users/{userId}/topup:
    post:
      tags: [billing, admin]
      operationId: topupAdminUser
      summary: Top up a user's balance
      description: |
        Credits a user's balance with the supplied amount (integer
        centimals). Requires billing.balance.adjust. Emits an audit
        event before + after the insert.
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BillingTopupRequest" }
      responses:
        "201":
          description: The new ledger row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingLedgerEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/admin/users/{userId}/refund:
    post:
      tags: [billing, admin]
      operationId: refundAdminUser
      summary: Refund a user (issue a credit row with source=refund)
      description: |
        Issues a credit row with source=refund referencing the original
        charge in metadata. Requires billing.balance.adjust. Emits an
        audit event before + after the insert.
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/BillingRefundRequest" }
      responses:
        "201":
          description: The new ledger row.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingLedgerEntry" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/admin/users/{userId}/ledger:
    get:
      tags: [billing, admin]
      operationId: listAdminUserLedger
      summary: List a user's ledger entries
      description: |
        Returns a paginated list of the user's ledger entries within
        the tenant in scope. Requires billing.ledger.read.
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - $ref: "#/components/parameters/PageLimit"
        - $ref: "#/components/parameters/PageOffset"
      responses:
        "200":
          description: A page of ledger entries.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingLedgerPage" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

  /api/v1/admin/users/{userId}/balance:
    get:
      tags: [billing, admin]
      operationId: getAdminUserBalance
      summary: Get a user's cached balance
      description: |
        Returns the cached per-user balance for the supplied user.
        Requires billing.balance.read.
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The cached balance.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }
    post:
      tags: [billing, admin]
      operationId: rebuildAdminUserBalance
      summary: Force-rebuild a user's balance cache
      description: |
        Recomputes the user's balance from the ledger and updates the
        cache. Requires billing.balance.adjust. Emits an audit event
        before + after.
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: The freshly-computed balance.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BillingBalance" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "501": { $ref: "#/components/responses/NotImplemented" }

# ---------------------------------------------------------------------------
# Components
# ---------------------------------------------------------------------------
components:
  # Standard headers reused across auth responses.
  headers:
    SetCookie:
      description: |
        An HTTP-only cookie carrying either the session id or the refresh
        token. Lahijan sets both on register/login/refresh and clears them on
        logout.
      schema:
        type: string
        examples: ["lahijan_session=...; HttpOnly; SameSite=Lax"]

  # Reusable query parameters.
  parameters:
    PageLimit:
      name: limit
      in: query
      description: Maximum number of items to return (1..200).
      schema: { type: integer, minimum: 1, maximum: 200, default: 50 }
    PageOffset:
      name: offset
      in: query
      description: Number of items to skip for pagination.
      schema: { type: integer, minimum: 0, default: 0 }
    ExportFormat:
      name: format
      in: query
      description: Output format for the export. Defaults to csv.
      schema: { type: string, enum: [csv, json], default: csv }

  # Standard error responses reused across the API.
  responses:
    BadRequest:
      description: The request was malformed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error:
              code: bad_request
              message: "request body failed validation"
              details:
                field: email
    Unauthorized:
      description: Authentication is required or has failed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error:
              code: unauthorized
              message: "authentication required"
    Forbidden:
      description: The caller is not permitted to perform this action.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error:
              code: forbidden
              message: "permission denied"
    NotFound:
      description: The requested resource does not exist.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error:
              code: not_found
              message: "resource not found"
    Conflict:
      description: The request conflicts with the current state of the resource.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error:
              code: conflict
              message: "an account with this email already exists"
    NotImplemented:
      description: The endpoint exists but is not implemented yet.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error:
              code: not_implemented
              message: "not implemented yet"
    InternalError:
      description: An unexpected error occurred.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          example:
            error:
              code: internal
              message: "internal server error"

  schemas:
    # The standard error envelope. Every error response in the API is shaped
    # exactly like this: { error: { code, message, details? } }.
    Error:
      type: object
      required: [error]
      additionalProperties: false
      properties:
        error:
          type: object
          required: [code, message]
          additionalProperties: false
          properties:
            code:
              type: string
              description: Machine-readable error code (snake_case).
              examples: [bad_request, unauthorized, forbidden, not_found, internal]
            message:
              type: string
              description: Human-readable error message, safe to show users.
            details:
              description: |
                Optional structured details about the error (e.g. per-field
                validation errors). Free-form object; consumers must not assume
                a fixed schema.
              type: object
              additionalProperties: true

    Health:
      type: object
      required: [status, version]
      additionalProperties: false
      properties:
        status:
          type: string
          enum: [ok, degraded]
          description: Coarse service health.
        version:
          type: string
          description: Build version of the running binary.

    Pong:
      type: object
      required: [pong]
      additionalProperties: false
      properties:
        pong:
          type: string
          description: RFC 3339 timestamp at which the server handled the request.
          format: date-time
          examples: ["2026-07-17T12:00:00Z"]

    User:
      type: object
      required: [id, email]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
          description: Unique user id.
        email:
          type: string
          format: email
          description: Unique user email.
        displayName:
          type: string
          description: Optional human-friendly display name.
          nullable: true
        memberships:
          type: array
          description: Tenant memberships held by this user.
          items:
            $ref: "#/components/schemas/Membership"

    Membership:
      type: object
      required: [tenantId, role]
      additionalProperties: false
      properties:
        tenantId:
          type: string
          format: uuid
        role:
          type: string
          description: Role held within the tenant (e.g. owner, admin, member).

    # ---------------------------------------------------------------------
    # Auth request/response schemas (WS-06).
    # ---------------------------------------------------------------------

    RegisterRequest:
      type: object
      required: [email, password]
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password
          description: Plaintext password; hashed with argon2id before storage.
        displayName:
          type: string
          nullable: true
        locale:
          type: string
          description: Preferred locale code (en, fa). Defaults to en.
          default: en

    LoginRequest:
      type: object
      required: [email, password]
      additionalProperties: false
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          format: password

    LogoutRequest:
      type: object
      additionalProperties: false
      description: |
        Optional. When omitted, the refresh-token cookie is used. Pass an
        explicit refresh token when logging out a non-browser client.
      properties:
        refreshToken:
          type: string

    RefreshRequest:
      type: object
      additionalProperties: false
      description: |
        Optional. When omitted, the refresh-token cookie is used. Pass an
        explicit refresh token when rotating from a non-browser client.
      properties:
        refreshToken:
          type: string

    TokenRequest:
      type: object
      required: [token]
      additionalProperties: false
      properties:
        token:
          type: string
          description: The single-use signed token from an email link.

    EmailRequest:
      type: object
      required: [email]
      additionalProperties: false
      properties:
        email:
          type: string
          format: email

    PasswordResetConfirmRequest:
      type: object
      required: [token, newPassword]
      additionalProperties: false
      properties:
        token:
          type: string
        newPassword:
          type: string
          format: password

    UpdateMeRequest:
      type: object
      additionalProperties: false
      properties:
        displayName:
          type: string
          nullable: true
        locale:
          type: string
        currentPassword:
          type: string
          format: password
          description: Required when changing the password.
        newPassword:
          type: string
          format: password
          description: When set, currentPassword must also be present.
        newEmail:
          type: string
          format: email
          description: |
            Triggers an email-change confirmation link; the email is not swapped
            until the link is consumed.

    AuthResponse:
      type: object
      required: [user]
      additionalProperties: false
      properties:
        user:
          $ref: "#/components/schemas/User"

    MFAChallengeRequired:
      type: object
      required: [pendingSessionToken, mfaRequired]
      additionalProperties: false
      properties:
        mfaRequired:
          type: boolean
          description: Always true; signals the 202 path.
        pendingSessionToken:
          type: string
          description: |
            Short-lived, single-use token. POST it to
            /api/v1/auth/mfa/challenge with the MFA code to obtain the
            real session cookies.
        enrolledFactors:
          type: array
          description: |
            The factors the user has enrolled. Lets the dashboard pick
            the default challenge UI (TOTP input vs WebAuthn prompt).
          items:
            type: string
            enum: [totp, webauthn]

    CreatePersonalAccessTokenRequest:
      type: object
      required: [name]
      additionalProperties: false
      properties:
        name:
          type: string
        scopes:
          type: array
          description: Permission slugs (scope.action) the PAT grants.
          items:
            type: string
        expiresAt:
          type: string
          format: date-time
          nullable: true

    PersonalAccessToken:
      type: object
      required: [id, name, scopes, createdAt]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        scopes:
          type: array
          items:
            type: string
        token:
          type: string
          description: The raw PAT. Present ONLY on create; never returned again.
        expiresAt:
          type: string
          format: date-time
          nullable: true
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time

    MessageResponse:
      type: object
      required: [message]
      additionalProperties: false
      properties:
        message:
          type: string
          description: Localized human-readable confirmation.

    # External identity (WS-07a + WS-07b): a link between a user and an
    # OAuth/OIDC/SAML IdP. Tokens are NOT included; only the provider key,
    # the IdP-stable subject, and the scopes the IdP granted at issue/refresh
    # time. For SAML identities the scopes array is empty and the optional
    # attributes map carries the snapshot from the most recent assertion.
    ExternalIdentity:
      type: object
      required: [id, provider, subject, scopes, createdAt]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
          description: The identity row id.
        provider:
          type: string
          description: |
            The provider key: a configured OAuth preset (google, github),
            "oidc:<config_key>" for an OIDC IdP, or "saml:<config_key>" for
            a SAML 2.0 IdP.
        subject:
          type: string
          description: |
            The IdP-stable subject identifier. For OAuth/OIDC this is the
            `sub` claim; for SAML it is the NameID.
        scopes:
          type: array
          description: |
            Scope strings the IdP granted at issue/refresh time. Empty for
            SAML identities (SAML has no notion of scopes).
          items:
            type: string
        attributes:
          type: object
          description: |
            SAML-only: snapshot of the attribute statement from the most
            recent assertion. Each key is a SAML attribute name (typically a
            URI like http://schemas.xmlsoap.org/ws/2005/05/identity/claims/
            emailaddress); each value is an array of strings (SAML attributes
            are multi-valued). NULL/absent for OAuth/OIDC identities.
          additionalProperties:
            type: array
            items:
              type: string
        expiresAt:
          type: string
          format: date-time
          nullable: true
          description: |
            When the access_token expires; NULL when non-expiring or for
            SAML identities (SAML assertions are short-lived and not stored).
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    # ---------------------------------------------------------------------
    # Audit schemas (WS-08).
    # ---------------------------------------------------------------------

    AuditEvent:
      type: object
      required: [id, action, resourceType, status, createdAt]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
          description: The audit row id.
        tenantId:
          type: string
          format: uuid
          nullable: true
          description: NULL for system-level events (e.g. login).
        actorUserId:
          type: string
          format: uuid
          nullable: true
          description: NULL when the actor is the system.
        actorType:
          type: string
          enum: [user, system, plugin]
        action:
          type: string
          description: |
            The privileged action slug, formatted scope.action
            (e.g. compute.instance.create). The localised label can be
            rendered via audit.action_<slug> i18n keys.
        resourceType:
          type: string
        resourceId:
          type: string
          format: uuid
          nullable: true
        status:
          type: string
          enum: [success, failure, pending]
          description: |
            The current status. When the event has outcome rows, this is the
            latest outcome's status; otherwise it is the row's initial status.
        requestId:
          type: string
          nullable: true
        metadata:
          type: object
          additionalProperties: true
          description: Free-form structured details recorded at emit time.
        createdAt:
          type: string
          format: date-time
        outcomes:
          type: array
          description: The outcome trail, newest first. Empty when Emit was fire-and-forget.
          items:
            $ref: "#/components/schemas/AuditOutcome"

    AuditOutcome:
      type: object
      required: [id, status, createdAt]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        status:
          type: string
          enum: [success, failure, pending]
        details:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time

    AuditPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AuditEvent" }
        total:
          type: integer
          format: int64
          description: Total events matching the filter (for pagination UI).
        limit:
          type: integer
        offset:
          type: integer

    # ---------------------------------------------------------------------
    # Admin job schemas (WS-09). Mirror River's river_job row shape, with a
    # few omissions (unique_key, unique_states) the admin UI doesn't need.
    # ---------------------------------------------------------------------

    AdminJob:
      type: object
      required: [id, kind, state, queue, priority, attempt, maxAttempts, createdAt, scheduledAt]
      additionalProperties: false
      properties:
        id:
          type: integer
          format: int64
          description: The River job id (bigserial).
        kind:
          type: string
          description: The stable "scope.action" job kind.
        state:
          type: string
          enum: [available, pending, retryable, running, scheduled, completed, cancelled, discarded]
        queue:
          type: string
        priority:
          type: integer
          minimum: 1
          maximum: 4
        attempt:
          type: integer
          description: The current attempt number (0 before the first run).
        maxAttempts:
          type: integer
        args:
          type: object
          additionalProperties: true
          description: The decoded job payload (JobArgs).
        attemptedBy:
          type: array
          items:
            type: string
        errors:
          type: array
          description: Per-attempt error trail, earliest first.
          items:
            $ref: "#/components/schemas/AdminJobError"
        tags:
          type: array
          items:
            type: string
        metadata:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        scheduledAt:
          type: string
          format: date-time
        attemptedAt:
          type: string
          format: date-time
          nullable: true
        finalizedAt:
          type: string
          format: date-time
          nullable: true

    AdminJobError:
      type: object
      required: [attempt, at, message]
      additionalProperties: false
      properties:
        attempt:
          type: integer
        at:
          type: string
          format: date-time
        message:
          type: string

    AdminJobPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AdminJob" }
        total:
          type: integer
          format: int64
        limit:
          type: integer
        offset:
          type: integer

    # ---------------------------------------------------------------------
    # Admin plugin schemas (WS-10a). The plugin row + grant shape used by
    # the /api/v1/admin/plugins/* endpoints. The wasm bytes are NOT exposed
    # — the admin UI shows the hash; downloads land in a follow-up WS if
    # needed. The manifest is round-tripped verbatim (parsed YAML -> JSON)
    # so the UI can render exactly what the plugin declared at upload time.
    # ---------------------------------------------------------------------

    AdminPlugin:
      type: object
      required: [id, name, version, status, wasmHash, wasmSize, createdAt, updatedAt]
      additionalProperties: false
      properties:
        id:
          type: string
          format: uuid
        tenantId:
          type: string
          format: uuid
          nullable: true
          description: NULL for platform-wide plugins.
        name:
          type: string
        version:
          type: string
        description:
          type: string
        wasmHash:
          type: string
          description: sha256 hex of wasmBytes; surfaced in the admin UI.
        wasmSize:
          type: integer
          format: int64
          description: Size of the stored .wasm bytes.
        manifest:
          type: object
          additionalProperties: true
          description: |
            The parsed lahijan.manifest.yaml kept verbatim. Shape documented
            in internal/app/lahijan/wasm/manifest/manifest.go.
        permissions:
          type: array
          description: Granted permission slugs (admin-approved at install).
          items:
            type: string
        status:
          type: string
          enum: [pending, active, disabled]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    AdminPluginPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AdminPlugin" }
        total:
          type: integer
          format: int64
        limit:
          type: integer
        offset:
          type: integer

    # ---------------------------------------------------------------------
    # Marketplace schemas (WS-10c). The marketplace index shape used by
    # /api/v1/admin/marketplace* + the upgrade-result bundle returned by
    # /api/v1/admin/plugins/upgrade/{name}.
    # ---------------------------------------------------------------------

    AdminMarketplaceEntry:
      type: object
      required: [name, version, source, sha256]
      additionalProperties: false
      properties:
        name:
          type: string
          description: Kebab-case plugin name (matches the manifest).
        version:
          type: string
          description: Semver version (matches the manifest).
        description:
          type: string
        author:
          type: string
        license:
          type: string
          description: SPDX identifier (Apache-2.0, MIT, ...).
        homepage:
          type: string
          format: uri
        permissions:
          type: array
          description: |
            Permission slugs the plugin requests. The admin must grant
            each one at install time; the upload parser rejects unknown
            slugs.
          items:
            type: string
        source:
          type: object
          required: [repo]
          additionalProperties: false
          properties:
            repo:
              type: string
              enum: [local, git]
              description: |
                `local` = the .wasm + manifest live under the marketplace
                root; `git` = clone the pinned git URL (future WS;
                signature verification required).
            path:
              type: string
              description: Subdirectory under the marketplace root (repo=local).
            gitURL:
              type: string
              format: uri
            gitRef:
              type: string
              description: Pinned git ref (branch, tag, or commit).
        sha256:
          type: string
          description: sha256 hex of the .wasm bytes; the installer verifies the download matches.

    AdminMarketplacePage:
      type: object
      required: [items]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/AdminMarketplaceEntry" }

    AdminPluginUpgradeResult:
      type: object
      required: [plugin, oldId, preservedGrants, droppedGrants, newPermissions]
      additionalProperties: false
      description: |
        The result bundle returned by the marketplace upgrade endpoint.
        The admin uses `newPermissions` to decide whether to grant each
        one (per WS-10c DoD "upgrade adds a permission -> admin is
        prompted to grant it"). `droppedGrants` are surfaced for
        visibility but cannot be undone — the new manifest no longer
        requests them.
      properties:
        plugin:
          description: The newly-installed plugin row (status=pending).
          allOf:
            - $ref: "#/components/schemas/AdminPlugin"
        oldId:
          type: string
          format: uuid
          description: The id of the previous plugin row (now hard-deleted).
        preservedGrants:
          type: array
          description: Grants carried forward from the prior version.
          items:
            type: string
        droppedGrants:
          type: array
          description: Grants no longer requested by the new manifest (silently dropped).
          items:
            type: string
        newPermissions:
          type: array
          description: |
            New manifest permissions the prior version did not have. The
            admin MUST grant each one (POST
            /api/v1/admin/plugins/{pluginId}/permissions/{perm}/grant)
            before calling /enable.
          items:
            type: string

    # ---------------------------------------------------------------------
    # MFA schemas (WS-07c)
    # ---------------------------------------------------------------------

    TOTPEnrollResponse:
      type: object
      required: [secret, provisioningUri]
      additionalProperties: false
      properties:
        secret:
          type: string
          description: |
            The raw base32 TOTP secret, shown ONCE so the user can enter
            it manually if they cannot scan the QR. Persisted encrypted
            at rest.
        provisioningUri:
          type: string
          description: |
            The otpauth:// URL the dashboard encodes as a QR code.

    TOTPVerifyRequest:
      type: object
      required: [code]
      additionalProperties: false
      properties:
        code:
          type: string
          description: The 6-digit code from the user's authenticator app.

    MFADisableRequest:
      type: object
      required: [currentPassword]
      additionalProperties: false
      properties:
        currentPassword:
          type: string
          format: password
          description: |
            The user's current password. Required so that a stolen
            session cookie cannot silently disarm MFA.

    RecoveryCodesBatch:
      type: object
      required: [codes]
      additionalProperties: false
      properties:
        codes:
          type: array
          description: |
            The raw recovery codes. Shown to the user EXACTLY ONCE;
            Lahijan cannot render them again.
          items:
            type: string

    RecoveryCodesListResponse:
      type: object
      required: [total, remaining]
      additionalProperties: false
      properties:
        total:
          type: integer
          description: Total codes the user has (used + unused).
        remaining:
          type: integer
          description: Number of unused codes remaining.
        rows:
          type: array
          description: Optional per-row metadata (no raw codes).
          items:
            type: object
            required: [id, used, createdAt]
            properties:
              id:
                type: string
                format: uuid
              used:
                type: boolean
              createdAt:
                type: string
                format: date-time

    WebAuthnBeginRequest:
      type: object
      additionalProperties: false
      properties:
        name:
          type: string
          description: Optional user-supplied label for the credential.

    WebAuthnBeginRegistrationResponse:
      type: object
      required: [publicKey, session]
      additionalProperties: false
      properties:
        publicKey:
          type: object
          description: |
            The PublicKeyCredentialCreationOptions the browser feeds to
            navigator.credentials.create(). Opaque to Lahijan.
        session:
          type: string
          description: |
            Opaque ceremony session token the dashboard MUST echo back
            at /finish in the matching field.

    WebAuthnFinishRegistrationRequest:
      type: object
      required: [session, response, name]
      additionalProperties: false
      properties:
        session:
          type: string
          description: The ceremony session returned by /register/begin.
        response:
          description: |
            The raw attestation response the browser POSTed. Carried
            verbatim to the WebAuthn library.
        name:
          type: string
          description: User-supplied label for the credential.

    WebAuthnLoginBeginRequest:
      type: object
      required: [pendingSessionToken]
      additionalProperties: false
      properties:
        pendingSessionToken:
          type: string
          description: The pending_session_token from /login or /oauth/callback.

    WebAuthnLoginBeginResponse:
      type: object
      required: [publicKey, session]
      additionalProperties: false
      properties:
        publicKey:
          type: object
          description: |
            The PublicKeyCredentialRequestOptions the browser feeds to
            navigator.credentials.get().
        session:
          type: string
          description: Opaque ceremony session for /login/finish.

    WebAuthnLoginFinishRequest:
      type: object
      required: [session, response]
      additionalProperties: false
      properties:
        session:
          type: string
        response:
          description: The raw assertion response from the browser.

    MFAChallengeRequest:
      type: object
      required: [pendingSessionToken, kind]
      additionalProperties: false
      properties:
        pendingSessionToken:
          type: string
          description: The token from /login when MFA is required.
        kind:
          type: string
          enum: [totp, recovery, webauthn]
          description: Which factor the user is challenging with.
        code:
          type: string
          description: |
            The 6-digit TOTP code (kind=totp) or the recovery code
            (kind=recovery). Empty when kind=webauthn.
        webauthnSession:
          type: string
          description: |
            The ceremony session from /me/mfa/webauthn/login/begin
            (kind=webauthn only).
        webauthnResponse:
          description: |
            The parsed assertion response from the browser
            (kind=webauthn only).

    # =========================================================================
    # Compute module schemas (WS-14)
    # =========================================================================

    ComputeInstance:
      type: object
      required: [id, tenantId, name, status, imageAlias, createdAt]
      additionalProperties: false
      properties:
        id:           { type: string, format: uuid }
        tenantId:     { type: string, format: uuid }
        name:         { type: string }
        type:
          type: string
          enum: [container, virtual-machine]
          description: Instance type. Defaults to container.
        status:
          type: string
          description: Cached Incus status string (Running, Stopped, Frozen, ...).
        statusCode:
          type: integer
          description: Numeric Incus status code (Running=103, Stopped=102).
        imageAlias:   { type: string }
        imageFingerprint:
          type: string
          description: Resolved image fingerprint (filled in at create success).
        profiles:
          type: array
          items: { type: string }
          description: Profile names applied to the instance.
        config:
          type: object
          additionalProperties: { type: string }
          description: Free-form config map (limits.cpu, limits.memory, ...).
        devices:
          type: object
          additionalProperties:
            type: object
            additionalProperties: { type: string }
          description: Per-device config map (root disk, nics, ...).
        description:  { type: string }
        createdAt:    { type: string, format: date-time }
        updatedAt:    { type: string, format: date-time }

    ComputeInstancePage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeInstance" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    ComputeInstanceCreateRequest:
      type: object
      required: [name, imageAlias]
      additionalProperties: false
      properties:
        name:        { type: string, minLength: 1, maxLength: 63 }
        type:
          type: string
          enum: [container, virtual-machine]
          default: container
        imageAlias:  { type: string, minLength: 1 }
        description: { type: string }
        config:
          type: object
          additionalProperties: { type: string }
        devices:
          type: object
          additionalProperties:
            type: object
            additionalProperties: { type: string }
        profiles:
          type: array
          items: { type: string }
          default: ["default"]

    ComputeInstanceUpdateRequest:
      type: object
      additionalProperties: false
      properties:
        description: { type: string }
        config:
          type: object
          additionalProperties: { type: string }
        devices:
          type: object
          additionalProperties:
            type: object
            additionalProperties: { type: string }
        profiles:
          type: array
          items: { type: string }

    ComputeExecRequest:
      type: object
      required: [command]
      additionalProperties: false
      properties:
        command:
          type: array
          items: { type: string }
          description: The argv to execute. Must be non-empty.
        environment:
          type: object
          additionalProperties: { type: string }
        user:  { type: integer, default: 0 }
        group: { type: integer, default: 0 }
        cwd:   { type: string }
        stdin:
          type: string
          description: Input bytes for the process stdin (UTF-8 text).

    ComputeExecResult:
      type: object
      required: [exitCode]
      additionalProperties: false
      properties:
        stdout:
          type: string
          description: Captured stdout (base64 if non-UTF-8).
        stderr:
          type: string
          description: Captured stderr (base64 if non-UTF-8).
        exitCode: { type: integer }

    ComputeImage:
      type: object
      required: [id, alias, source]
      additionalProperties: false
      properties:
        id:           { type: string, format: uuid }
        alias:        { type: string }
        source:
          type: string
          enum: [featured, custom]
        fingerprint:  { type: string }
        type:
          type: string
          enum: [container, virtual-machine]
        architecture: { type: string }
        sizeBytes:    { type: integer, format: int64 }
        description:  { type: string }
        createdAt:    { type: string, format: date-time }
        updatedAt:    { type: string, format: date-time }

    ComputeImagePage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeImage" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    ComputeImageUploadRequest:
      type: object
      required: [alias, fingerprint]
      additionalProperties: false
      properties:
        alias:        { type: string, minLength: 1, maxLength: 63 }
        fingerprint:  { type: string, minLength: 1 }
        type:
          type: string
          enum: [container, virtual-machine]
          default: container
        architecture: { type: string }
        sizeBytes:    { type: integer, format: int64 }
        properties:
          type: object
          additionalProperties: { type: string }
        description:  { type: string }

    ComputeProfile:
      type: object
      required: [id, name]
      additionalProperties: false
      properties:
        id:          { type: string, format: uuid }
        name:        { type: string }
        description: { type: string }
        config:
          type: object
          additionalProperties: { type: string }
        devices:
          type: object
          additionalProperties:
            type: object
            additionalProperties: { type: string }
        createdAt:   { type: string, format: date-time }
        updatedAt:   { type: string, format: date-time }

    ComputeProfilePage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeProfile" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    ComputeProfileCreateRequest:
      type: object
      required: [name]
      additionalProperties: false
      properties:
        name:        { type: string, minLength: 1, maxLength: 63 }
        description: { type: string }
        config:
          type: object
          additionalProperties: { type: string }
        devices:
          type: object
          additionalProperties:
            type: object
            additionalProperties: { type: string }

    ComputeNetwork:
      type: object
      required: [id, name, type]
      additionalProperties: false
      properties:
        id:          { type: string, format: uuid }
        name:        { type: string }
        description: { type: string }
        type:        { type: string }
        config:
          type: object
          additionalProperties: { type: string }
        aclNames:
          type: array
          items: { type: string }
        forwardNames:
          type: array
          items: { type: string }
        createdAt:   { type: string, format: date-time }
        updatedAt:   { type: string, format: date-time }

    ComputeNetworkPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeNetwork" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    ComputeNetworkCreateRequest:
      type: object
      required: [name]
      additionalProperties: false
      properties:
        name:        { type: string, minLength: 1, maxLength: 63 }
        description: { type: string }
        type:
          type: string
          default: bridge
        config:
          type: object
          additionalProperties: { type: string }

    ComputeStorageVolume:
      type: object
      required: [id, name, poolName]
      additionalProperties: false
      properties:
        id:          { type: string, format: uuid }
        name:        { type: string }
        description: { type: string }
        type:        { type: string }
        poolName:    { type: string }
        config:
          type: object
          additionalProperties: { type: string }
        createdAt:   { type: string, format: date-time }
        updatedAt:   { type: string, format: date-time }

    ComputeStorageVolumePage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeStorageVolume" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    ComputeStorageVolumeCreateRequest:
      type: object
      required: [name]
      additionalProperties: false
      properties:
        name:        { type: string, minLength: 1, maxLength: 63 }
        description: { type: string }
        poolName:    { type: string, default: default }
        config:
          type: object
          additionalProperties: { type: string }

    # =========================================================================
    # Compute snapshots + backups + policies schemas (WS-25)
    # =========================================================================

    ComputeSnapshot:
      type: object
      required: [id, instanceId, name, stateful, createdAt]
      additionalProperties: false
      properties:
        id:          { type: string, format: uuid }
        instanceId:  { type: string, format: uuid }
        name:        { type: string }
        description: { type: string }
        stateful:    { type: boolean }
        sizeBytes:   { type: integer, format: int64 }
        expiresAt:   { type: string, format: date-time, nullable: true }
        policyId:    { type: string, format: uuid, nullable: true }
        createdAt:   { type: string, format: date-time }
        updatedAt:   { type: string, format: date-time }

    ComputeSnapshotPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeSnapshot" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    ComputeSnapshotCreateRequest:
      type: object
      required: [name]
      additionalProperties: false
      properties:
        name:        { type: string, minLength: 1, maxLength: 63 }
        description: { type: string }
        stateful:    { type: boolean, default: false }

    ComputeSnapshotRestoreRequest:
      type: object
      additionalProperties: false
      properties:
        stateful: { type: boolean, default: false }

    ComputeSnapshotPolicy:
      type: object
      required: [id, name, cadence, retainCount, enabled, createdAt]
      additionalProperties: false
      properties:
        id:          { type: string, format: uuid }
        instanceId:  { type: string, format: uuid, nullable: true }
        name:        { type: string }
        cadence:     { type: string }
        retainCount: { type: integer }
        targetId:    { type: string, format: uuid, nullable: true }
        enabled:     { type: boolean }
        lastRunAt:   { type: string, format: date-time, nullable: true }
        nextRunAt:   { type: string, format: date-time, nullable: true }
        createdAt:   { type: string, format: date-time }
        updatedAt:   { type: string, format: date-time }

    ComputeSnapshotPolicyPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeSnapshotPolicy" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    ComputeSnapshotPolicyCreateRequest:
      type: object
      required: [name, cadence]
      additionalProperties: false
      properties:
        name:        { type: string, minLength: 1, maxLength: 63 }
        instanceId:  { type: string, format: uuid, nullable: true }
        cadence:     { type: string, description: "ISO 8601 duration: PT1H, P1D, P1W, P1DT2H..." }
        retainCount: { type: integer, minimum: 1, default: 7 }
        targetId:    { type: string, format: uuid, nullable: true }
        enabled:     { type: boolean, default: true }

    ComputeSnapshotPolicyUpdateRequest:
      type: object
      required: [name, cadence]
      additionalProperties: false
      properties:
        name:        { type: string, minLength: 1, maxLength: 63 }
        instanceId:  { type: string, format: uuid, nullable: true }
        cadence:     { type: string }
        retainCount: { type: integer, minimum: 1 }
        targetId:    { type: string, format: uuid, nullable: true }
        enabled:     { type: boolean }

    ComputeBackupTarget:
      type: object
      required: [id, name, kind, enabled, createdAt]
      additionalProperties: false
      properties:
        id:          { type: string, format: uuid }
        name:        { type: string }
        kind:
          type: string
          enum: [s3, nfs, ssh]
        description: { type: string }
        config:
          type: object
          additionalProperties: true
          description: Per-kind non-sensitive config (endpoint, bucket, path, host, ...).
        enabled:   { type: boolean }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    ComputeBackupTargetPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeBackupTarget" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    ComputeBackupTargetCreateRequest:
      type: object
      required: [name, kind, config, secret]
      additionalProperties: false
      properties:
        name:        { type: string, minLength: 1, maxLength: 63 }
        kind:
          type: string
          enum: [s3, nfs, ssh]
        description: { type: string }
        config:
          type: object
          additionalProperties: true
        secret:
          type: object
          additionalProperties: true
          description: Per-kind credentials envelope (S3 access keys, SSH private key, ...). Encrypted at rest.
        enabled: { type: boolean, default: true }

    ComputeBackup:
      type: object
      required: [id, snapshotId, instanceId, targetId, status, createdAt]
      additionalProperties: false
      properties:
        id:             { type: string, format: uuid }
        snapshotId:     { type: string, format: uuid }
        instanceId:     { type: string, format: uuid }
        targetId:       { type: string, format: uuid }
        remoteLocation: { type: string }
        sizeBytes:      { type: integer, format: int64 }
        checksumSha256: { type: string }
        status:
          type: string
          enum: [pending, uploading, completed, failed]
        errorMessage: { type: string }
        createdAt:    { type: string, format: date-time }
        updatedAt:    { type: string, format: date-time }

    ComputeBackupPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/ComputeBackup" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    # =========================================================================
    # DNS module schemas (WS-15)
    # =========================================================================

    DNSZone:
      type: object
      required: [id, tenantId, name, canonicalId, kind, isDnssecEnabled, createdAt]
      additionalProperties: false
      properties:
        id:             { type: string, format: uuid }
        tenantId:       { type: string, format: uuid }
        canonicalId:
          type: string
          description: The PDNS-assigned canonical id (typically the canonical name lowercased). Unique across the platform.
        name:
          type: string
          description: Canonical zone name with trailing dot ("example.com.").
        kind:
          type: string
          enum: [Native, Master, Slave]
          description: Zone topology. Lahijan uses Native by default.
        description:    { type: string }
        isDnssecEnabled:
          type: boolean
          description: Cached DNSSEC state. Flipped by the DNS service on enable/disable.
        isAxfrEnabled:
          type: boolean
          description: Cached AXFR state. Off by default per WS-12.
        createdAt:      { type: string, format: date-time }
        updatedAt:      { type: string, format: date-time }

    DNSZonePage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/DNSZone" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    DNSZoneCreateRequest:
      type: object
      required: [name]
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          description: Canonical zone name; must end with a dot and be lowercase.
        description: { type: string }
        kind:
          type: string
          enum: [Native, Master, Slave]
          default: Native

    DNSZoneUpdateRequest:
      type: object
      additionalProperties: false
      properties:
        description: { type: string }
        kind:
          type: string
          enum: [Native, Master, Slave]

    DNSRecord:
      type: object
      required: [id, tenantId, zoneId, name, type, content, ttl, createdAt]
      additionalProperties: false
      properties:
        id:       { type: string, format: uuid }
        tenantId: { type: string, format: uuid }
        zoneId:   { type: string, format: uuid }
        name:
          type: string
          description: Canonical record name with trailing dot.
        type:
          type: string
          description: DNS record type.
        content:
          type: string
          description: Zone-file wire form.
        ttl:
          type: integer
          description: TTL in seconds.
        prio:
          type: integer
          description: Priority for MX / SRV; 0 for types with no priority.
        disabled:
          type: boolean
          description: When true the record is served commented-out.
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    DNSRecordPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/DNSRecord" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    DNSRecordCreateRequest:
      type: object
      required: [name, type, content]
      additionalProperties: false
      properties:
        name:
          type: string
          minLength: 1
          description: Canonical record name with trailing dot; must belong to the zone.
        type:
          type: string
          enum: [A, AAAA, CAA, CNAME, DS, MX, NS, PTR, SOA, SRV, TLSA, TXT]
          description: DNS record type. Validated per-type.
        content:
          type: string
          description: Zone-file wire form. Validated per-type.
        ttl:
          type: integer
          minimum: 300
          maximum: 86400
          default: 3600
          description: TTL in seconds; clamped to [300, 86400].
        disabled:
          type: boolean
          default: false

    DNSRecordUpdateRequest:
      type: object
      additionalProperties: false
      properties:
        content: { type: string }
        ttl:
          type: integer
          minimum: 300
          maximum: 86400
        disabled: { type: boolean }

    DNSTemplate:
      type: object
      required: [id, name, description, records]
      additionalProperties: false
      properties:
        id:          { type: string }
        name:        { type: string }
        description: { type: string }
        records:
          type: array
          items: { $ref: "#/components/schemas/DNSTemplateRecord" }

    DNSTemplateRecord:
      type: object
      required: [name, type, content]
      additionalProperties: false
      properties:
        name:
          type: string
          description: Canonical record name. "%s" expands to the zone id; "%w" to the zone id without the trailing dot.
        type:
          type: string
          enum: [A, AAAA, CAA, CNAME, DS, MX, NS, PTR, SOA, SRV, TLSA, TXT]
        content:
          type: string
          description: Zone-file wire form. Same "%s"/"%w" expansion as Name.
        ttl:
          type: integer
          minimum: 300
          maximum: 86400

    ApplyDNSTemplateRequest:
      type: object
      required: [templateId]
      additionalProperties: false
      properties:
        templateId:
          type: string
          description: The template id from GET /api/v1/dns/templates.

    ApplyDNSTemplateResult:
      type: object
      required: [applied]
      additionalProperties: false
      properties:
        applied:
          type: integer
          description: Number of records upserted by the template.

    # -------------------------------------------------------------------------
    # Object Storage (WS-16)
    # -------------------------------------------------------------------------

    StorageBucket:
      type: object
      required: [id, tenantId, name, slug, ownerId, quotaBytes, quotaObjects, bytesUsed, objectsUsed, createdAt]
      additionalProperties: false
      properties:
        id:             { type: string, format: uuid }
        tenantId:       { type: string, format: uuid }
        name:
          type: string
          description: Canonical "<tenant-uuid>-<slug>" form (ADR-0011); globally unique.
        slug:
          type: string
          description: User-picked slug portion (1-26 lowercase alphanumeric + dashes).
        ownerId:
          type: string
          format: uuid
          description: The user who created (and therefore owns) the bucket.
        label:
          type: string
          description: Optional user-facing label.
        description:
          type: string
          description: Optional free-form description.
        quotaBytes:
          type: integer
          format: int64
          description: Per-bucket size ceiling in bytes; 0 = unlimited. Enforced server-side.
        quotaObjects:
          type: integer
          format: int64
          description: Per-bucket object-count ceiling; 0 = unlimited.
        bytesUsed:
          type: integer
          format: int64
          description: Cached total object size in bytes; refreshed by the metering job.
        objectsUsed:
          type: integer
          format: int64
          description: Cached total object count; refreshed by the metering job.
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    StorageBucketPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/StorageBucket" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    StorageBucketCreateRequest:
      type: object
      required: [slug]
      additionalProperties: false
      properties:
        slug:
          type: string
          minLength: 1
          maxLength: 26
          pattern: '^[a-z0-9][a-z0-9-]{0,24}[a-z0-9]$|^[a-z0-9]$'
          description: User-picked bucket slug per ADR-0011.
        label:       { type: string, maxLength: 100 }
        description: { type: string }
        quotaBytes:
          type: integer
          format: int64
          minimum: 0
          default: 0
          description: Per-bucket size ceiling in bytes; 0 = unlimited.
        quotaObjects:
          type: integer
          format: int64
          minimum: 0
          default: 0
          description: Per-bucket object-count ceiling; 0 = unlimited.

    StorageBucketUpdateRequest:
      type: object
      additionalProperties: false
      properties:
        label:
          type: string
          maxLength: 100
        description:
          type: string

    StorageCredential:
      type: object
      required: [id, bucketId, tenantId, userId, accessKeyId, actions, createdAt]
      additionalProperties: false
      properties:
        id:          { type: string, format: uuid }
        bucketId:    { type: string, format: uuid }
        tenantId:    { type: string, format: uuid }
        userId:      { type: string, format: uuid }
        accessKeyId:
          type: string
          description: S3 access key string. Globally unique across the platform.
        label:
          type: string
          description: User-supplied label so the user can tell credentials apart.
        actions:
          type: array
          items:
            type: string
            enum: [Read, Write, List, Tagging, Admin]
          description: High-level actions the credential permits on the bucket.
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
        expiresAt:
          type: string
          format: date-time
          nullable: true
          description: Optional expiry. The metering job revokes the credential past this.
        createdAt: { type: string, format: date-time }
        revokedAt:
          type: string
          format: date-time
          nullable: true

    StorageCredentialWithSecret:
      type: object
      required: [credential, secretKey]
      additionalProperties: false
      properties:
        credential:
          $ref: "#/components/schemas/StorageCredential"
        secretKey:
          type: string
          description: |
            The plaintext S3 secret key. SENSITIVE — shown exactly once at
            mint time. Store it securely; Lahijan cannot recover it.

    StorageCredentialPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/StorageCredential" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    StorageCredentialCreateRequest:
      type: object
      required: [actions]
      additionalProperties: false
      properties:
        label:
          type: string
          maxLength: 100
        actions:
          type: array
          minItems: 1
          items:
            type: string
            enum: [Read, Write, List, Tagging, Admin]
          description: At least one action is required.
        expiresInSeconds:
          type: integer
          minimum: 1
          description: |
            Optional lifetime in seconds. When set, the credential expires
            at created_at + expiresInSeconds; the metering job revokes it
            past that point. Absent means "no expiry".

    StoragePresignRequest:
      type: object
      required: [method]
      additionalProperties: false
      properties:
        method:
          type: string
          enum: [GET, PUT]
          description: HTTP method the URL is signed for.
        key:
          type: string
          description: Object key within the bucket. Empty targets the bucket root.
        expiresInSeconds:
          type: integer
          minimum: 1
          maximum: 86400
          default: 3600
          description: URL lifetime in seconds; clamped to [1, 86400].

    StoragePresignResult:
      type: object
      required: [url, method, bucket, expiresAt]
      additionalProperties: false
      properties:
        url:
          type: string
          description: The SigV4-signed URL the user opens directly against the backend.
        method:
          type: string
          enum: [GET, PUT]
        bucket:
          type: string
          description: Canonical bucket name the URL targets.
        key:
          type: string
          description: Object key within the bucket.
        expiresAt:
          type: string
          format: date-time
          description: When the signature stops being valid.

    StorageQuotaRequest:
      type: object
      required: [quotaBytes, quotaObjects]
      additionalProperties: false
      properties:
        quotaBytes:
          type: integer
          format: int64
          minimum: 0
          description: Per-bucket size ceiling in bytes; 0 clears the size ceiling.
        quotaObjects:
          type: integer
          format: int64
          minimum: 0
          description: Per-bucket object-count ceiling; 0 clears the count ceiling.

    StorageBucketUsage:
      type: object
      required: [quotaBytes, quotaObjects, bytesUsed, objectsUsed]
      additionalProperties: false
      properties:
        quotaBytes:
          type: integer
          format: int64
          description: Configured size ceiling; 0 = unlimited.
        quotaObjects:
          type: integer
          format: int64
          description: Configured object-count ceiling; 0 = unlimited.
        bytesUsed:
          type: integer
          format: int64
          description: Cached total object size in bytes.
        objectsUsed:
          type: integer
          format: int64
          description: Cached total object count.

    # -----------------------------------------------------------------
    # Billing & metering schemas (WS-17). All amounts in integer
    # centimals; the ledger is append-only.
    # -----------------------------------------------------------------

    BillingBalance:
      type: object
      required: [tenantId, userId, balanceCents, currency, lastEntryAt, updatedAt]
      additionalProperties: false
      properties:
        tenantId:    { type: string, format: uuid }
        userId:      { type: string, format: uuid }
        balanceCents:
          type: integer
          format: int64
          description: Cached balance in integer centimals. Can be slightly negative during the grace window.
        currency:
          type: string
          description: ISO 4217 currency code (USD today; multi-currency is Phase 7).
        lastEntryAt:
          type: string
          format: date-time
          description: Timestamp of the latest ledger entry incorporated into balanceCents.
        updatedAt:
          type: string
          format: date-time
          description: Cache refresh timestamp; should be < 60s stale per the WS-17 DoD.

    BillingLedgerEntry:
      type: object
      required: [id, tenantId, userId, type, amountCents, currency, source, createdAt]
      additionalProperties: false
      properties:
        id:           { type: string, format: uuid }
        tenantId:     { type: string, format: uuid }
        userId:       { type: string, format: uuid }
        type:
          type: string
          enum: [credit, debit]
          description: credit (topup, refund) increases the balance; debit (charge) decreases it.
        amountCents:
          type: integer
          format: int64
          description: Amount in integer centimals; always positive.
        currency:     { type: string }
        source:
          type: string
          enum: [topup, charge, refund, adjustment]
        reference:    { type: string }
        idempotencyKey:
          type: string
          nullable: true
          description: Idempotency key for metering de-duplication; NULL for admin rows.
        metadata:
          type: object
          additionalProperties: true
          description: Structured details (resource type, period, breakdown, ...).
        createdAt:    { type: string, format: date-time }

    BillingLedgerPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BillingLedgerEntry" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    BillingUsageEvent:
      type: object
      required: [id, tenantId, userId, resourceType, qty, unit, startedAt, endedAt]
      additionalProperties: false
      properties:
        id:           { type: string, format: uuid }
        tenantId:     { type: string, format: uuid }
        userId:       { type: string, format: uuid }
        resourceType: { type: string }
        qty:
          type: integer
          format: int64
          description: Integer quantity; always positive.
        unit:         { type: string }
        startedAt:    { type: string, format: date-time }
        endedAt:      { type: string, format: date-time }
        idempotencyKey:
          type: string
          nullable: true
        createdAt:    { type: string, format: date-time }

    BillingUsagePage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BillingUsageEvent" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    BillingPrice:
      type: object
      required: [id, tenantId, resourceType, unit, priceCents, currency, effectiveFrom]
      additionalProperties: false
      properties:
        id:           { type: string, format: uuid }
        tenantId:     { type: string, format: uuid }
        resourceType: { type: string }
        unit:         { type: string }
        priceCents:
          type: integer
          format: int64
          description: Unit price in integer centimals.
        currency:     { type: string }
        effectiveFrom: { type: string, format: date-time }
        effectiveTo:
          type: string
          format: date-time
          nullable: true
          description: When this price stops being effective. NULL = currently in effect.
        createdAt:    { type: string, format: date-time }
        updatedAt:    { type: string, format: date-time }

    BillingPricePage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BillingPrice" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    BillingPriceUpsertRequest:
      type: object
      required: [resourceType, unit, priceCents]
      additionalProperties: false
      properties:
        resourceType: { type: string }
        unit:         { type: string }
        priceCents:
          type: integer
          format: int64
          minimum: 0
        currency:
          type: string
          default: USD
          description: ISO 4217 currency code. Defaults to USD.
        effectiveFrom:
          type: string
          format: date-time
          description: When the new price takes effect. Defaults to now.

    BillingTopupRequest:
      type: object
      required: [amountCents]
      additionalProperties: false
      properties:
        amountCents:
          type: integer
          format: int64
          minimum: 1
          description: The credit amount in integer centimals.
        currency:
          type: string
          default: USD
        reference:
          type: string
          maxLength: 200
          description: Optional human-readable reference (invoice id, ...).

    BillingRefundRequest:
      type: object
      required: [amountCents]
      additionalProperties: false
      properties:
        amountCents:
          type: integer
          format: int64
          minimum: 1
        currency:
          type: string
          default: USD
        reference:
          type: string
          maxLength: 200
        chargeLedgerId:
          type: string
          format: uuid
          nullable: true
          description: Optional reference to the original charge ledger row.

    BillingReceipt:
      type: object
      required: [id, tenantId, userId, periodStart, periodEnd, totalCents, currency, status, createdAt]
      additionalProperties: false
      properties:
        id:           { type: string, format: uuid }
        tenantId:     { type: string, format: uuid }
        userId:       { type: string, format: uuid }
        periodStart:  { type: string, format: date-time }
        periodEnd:    { type: string, format: date-time }
        totalCents:
          type: integer
          format: int64
          description: Total charged over the period in integer centimals.
        currency:     { type: string }
        status:
          type: string
          enum: [pending, ready]
        createdAt:    { type: string, format: date-time }
        updatedAt:    { type: string, format: date-time }

    BillingReceiptPage:
      type: object
      required: [items, total, limit, offset]
      additionalProperties: false
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/BillingReceipt" }
        total:  { type: integer }
        limit:  { type: integer }
        offset: { type: integer }

    BillingReceiptGenerateRequest:
      type: object
      required: [periodStart, periodEnd]
      additionalProperties: false
      properties:
        periodStart: { type: string, format: date-time }
        periodEnd:   { type: string, format: date-time }
