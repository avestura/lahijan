// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: audit_log.sql

package gen

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
)

const countAuditLogForTenant = `-- name: CountAuditLogForTenant :one
SELECT count(*) FROM audit_log WHERE tenant_id = $1
`

// : tenant-scoped
func (q *Queries) CountAuditLogForTenant(ctx context.Context, tenantID *uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countAuditLogForTenant, tenantID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createAuditLog = `-- name: CreateAuditLog :one

INSERT INTO audit_log (
    tenant_id,
    actor_user_id,
    actor_type,
    action,
    resource_type,
    resource_id,
    status,
    request_id,
    metadata
)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, tenant_id, actor_user_id, actor_type, action, resource_type, resource_id, status, request_id, metadata, created_at
`

type CreateAuditLogParams struct {
	TenantID     *uuid.UUID      `json:"tenant_id"`
	ActorUserID  *uuid.UUID      `json:"actor_user_id"`
	ActorType    string          `json:"actor_type"`
	Action       string          `json:"action"`
	ResourceType string          `json:"resource_type"`
	ResourceID   *uuid.UUID      `json:"resource_id"`
	Status       string          `json:"status"`
	RequestID    *string         `json:"request_id"`
	Metadata     json.RawMessage `json:"metadata"`
}

// Audit log: append-only. tenant_id is nullable for system-level events.
// The audit_log_block_mutation trigger (migration 0005) rejects UPDATE/DELETE,
// so this query file intentionally exposes only INSERT and SELECT.
// Optional fields use explicit params; the repository wrapper supplies defaults.
func (q *Queries) CreateAuditLog(ctx context.Context, arg CreateAuditLogParams) (AuditLog, error) {
	row := q.db.QueryRow(ctx, createAuditLog,
		arg.TenantID,
		arg.ActorUserID,
		arg.ActorType,
		arg.Action,
		arg.ResourceType,
		arg.ResourceID,
		arg.Status,
		arg.RequestID,
		arg.Metadata,
	)
	var i AuditLog
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.ActorUserID,
		&i.ActorType,
		&i.Action,
		&i.ResourceType,
		&i.ResourceID,
		&i.Status,
		&i.RequestID,
		&i.Metadata,
		&i.CreatedAt,
	)
	return i, err
}

const getAuditLog = `-- name: GetAuditLog :one
SELECT id, tenant_id, actor_user_id, actor_type, action, resource_type, resource_id, status, request_id, metadata, created_at FROM audit_log WHERE id = $1
`

func (q *Queries) GetAuditLog(ctx context.Context, id uuid.UUID) (AuditLog, error) {
	row := q.db.QueryRow(ctx, getAuditLog, id)
	var i AuditLog
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.ActorUserID,
		&i.ActorType,
		&i.Action,
		&i.ResourceType,
		&i.ResourceID,
		&i.Status,
		&i.RequestID,
		&i.Metadata,
		&i.CreatedAt,
	)
	return i, err
}

const listAuditLogForTenant = `-- name: ListAuditLogForTenant :many
SELECT id, tenant_id, actor_user_id, actor_type, action, resource_type, resource_id, status, request_id, metadata, created_at FROM audit_log
WHERE tenant_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type ListAuditLogForTenantParams struct {
	TenantID *uuid.UUID `json:"tenant_id"`
	Limit    int32      `json:"limit"`
	Offset   int32      `json:"offset"`
}

// : tenant-scoped
func (q *Queries) ListAuditLogForTenant(ctx context.Context, arg ListAuditLogForTenantParams) ([]AuditLog, error) {
	rows, err := q.db.Query(ctx, listAuditLogForTenant, arg.TenantID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AuditLog{}
	for rows.Next() {
		var i AuditLog
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.ActorUserID,
			&i.ActorType,
			&i.Action,
			&i.ResourceType,
			&i.ResourceID,
			&i.Status,
			&i.RequestID,
			&i.Metadata,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listAuditLogGlobal = `-- name: ListAuditLogGlobal :many
SELECT id, tenant_id, actor_user_id, actor_type, action, resource_type, resource_id, status, request_id, metadata, created_at FROM audit_log
ORDER BY created_at DESC
LIMIT $1 OFFSET $2
`

type ListAuditLogGlobalParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

// : admin-only; system-wide query, not tenant-scoped
func (q *Queries) ListAuditLogGlobal(ctx context.Context, arg ListAuditLogGlobalParams) ([]AuditLog, error) {
	rows, err := q.db.Query(ctx, listAuditLogGlobal, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []AuditLog{}
	for rows.Next() {
		var i AuditLog
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.ActorUserID,
			&i.ActorType,
			&i.Action,
			&i.ResourceType,
			&i.ResourceID,
			&i.Status,
			&i.RequestID,
			&i.Metadata,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
