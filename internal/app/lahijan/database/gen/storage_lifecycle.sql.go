// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: storage_lifecycle.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countStorageLifecycleRules = `-- name: CountStorageLifecycleRules :one
SELECT count(*) FROM storage_lifecycle_rules
WHERE tenant_id = $1 AND bucket_id = $2
`

type CountStorageLifecycleRulesParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	BucketID uuid.UUID `json:"bucket_id"`
}

// : tenant-scoped
func (q *Queries) CountStorageLifecycleRules(ctx context.Context, arg CountStorageLifecycleRulesParams) (int64, error) {
	row := q.db.QueryRow(ctx, countStorageLifecycleRules, arg.TenantID, arg.BucketID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createStorageLifecycleRule = `-- name: CreateStorageLifecycleRule :one

INSERT INTO storage_lifecycle_rules (
    tenant_id, bucket_id, rule_id, status, action,
    days, date_at, storage_class, prefix
)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, tenant_id, bucket_id, rule_id, status, action, days, date_at, storage_class, prefix, created_at, updated_at
`

type CreateStorageLifecycleRuleParams struct {
	TenantID     uuid.UUID  `json:"tenant_id"`
	BucketID     uuid.UUID  `json:"bucket_id"`
	RuleID       string     `json:"rule_id"`
	Status       string     `json:"status"`
	Action       string     `json:"action"`
	Days         *int32     `json:"days"`
	DateAt       *time.Time `json:"date_at"`
	StorageClass *string    `json:"storage_class"`
	Prefix       *string    `json:"prefix"`
}

// Storage lifecycle rules: tenant-scoped per-bucket S3 lifecycle rules
// (WS-29, ADR-0036). Every query is tenant-scoped via WithTenant
// (database/tenant.go) so a cross-tenant bucket_id surfaces as
// ErrNoRows, never as the row itself.
// : tenant-scoped
func (q *Queries) CreateStorageLifecycleRule(ctx context.Context, arg CreateStorageLifecycleRuleParams) (StorageLifecycleRule, error) {
	row := q.db.QueryRow(ctx, createStorageLifecycleRule,
		arg.TenantID,
		arg.BucketID,
		arg.RuleID,
		arg.Status,
		arg.Action,
		arg.Days,
		arg.DateAt,
		arg.StorageClass,
		arg.Prefix,
	)
	var i StorageLifecycleRule
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.BucketID,
		&i.RuleID,
		&i.Status,
		&i.Action,
		&i.Days,
		&i.DateAt,
		&i.StorageClass,
		&i.Prefix,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteAllStorageLifecycleRulesForBucket = `-- name: DeleteAllStorageLifecycleRulesForBucket :exec
DELETE FROM storage_lifecycle_rules
WHERE tenant_id = $1 AND bucket_id = $2
`

type DeleteAllStorageLifecycleRulesForBucketParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	BucketID uuid.UUID `json:"bucket_id"`
}

// : tenant-scoped
// Bulk-delete used by the service layer when a user replaces the whole
// policy via PUT /lifecycle (the new policy is then written rule by
// rule in the same transaction).
func (q *Queries) DeleteAllStorageLifecycleRulesForBucket(ctx context.Context, arg DeleteAllStorageLifecycleRulesForBucketParams) error {
	_, err := q.db.Exec(ctx, deleteAllStorageLifecycleRulesForBucket, arg.TenantID, arg.BucketID)
	return err
}

const deleteStorageLifecycleRule = `-- name: DeleteStorageLifecycleRule :exec
DELETE FROM storage_lifecycle_rules
WHERE tenant_id = $1 AND id = $2
`

type DeleteStorageLifecycleRuleParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	ID       uuid.UUID `json:"id"`
}

// : tenant-scoped
func (q *Queries) DeleteStorageLifecycleRule(ctx context.Context, arg DeleteStorageLifecycleRuleParams) error {
	_, err := q.db.Exec(ctx, deleteStorageLifecycleRule, arg.TenantID, arg.ID)
	return err
}

const getStorageLifecycleRule = `-- name: GetStorageLifecycleRule :one
SELECT id, tenant_id, bucket_id, rule_id, status, action, days, date_at, storage_class, prefix, created_at, updated_at FROM storage_lifecycle_rules
WHERE tenant_id = $1 AND bucket_id = $2 AND rule_id = $3
`

type GetStorageLifecycleRuleParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	BucketID uuid.UUID `json:"bucket_id"`
	RuleID   string    `json:"rule_id"`
}

// : tenant-scoped
// Lookup by (bucket_id, rule_id) — the natural key the storage service
// uses on every privileged call (UpdateRule / DeleteRule).
func (q *Queries) GetStorageLifecycleRule(ctx context.Context, arg GetStorageLifecycleRuleParams) (StorageLifecycleRule, error) {
	row := q.db.QueryRow(ctx, getStorageLifecycleRule, arg.TenantID, arg.BucketID, arg.RuleID)
	var i StorageLifecycleRule
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.BucketID,
		&i.RuleID,
		&i.Status,
		&i.Action,
		&i.Days,
		&i.DateAt,
		&i.StorageClass,
		&i.Prefix,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getStorageLifecycleRuleByID = `-- name: GetStorageLifecycleRuleByID :one
SELECT id, tenant_id, bucket_id, rule_id, status, action, days, date_at, storage_class, prefix, created_at, updated_at FROM storage_lifecycle_rules
WHERE tenant_id = $1 AND id = $2
`

type GetStorageLifecycleRuleByIDParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	ID       uuid.UUID `json:"id"`
}

// : tenant-scoped
func (q *Queries) GetStorageLifecycleRuleByID(ctx context.Context, arg GetStorageLifecycleRuleByIDParams) (StorageLifecycleRule, error) {
	row := q.db.QueryRow(ctx, getStorageLifecycleRuleByID, arg.TenantID, arg.ID)
	var i StorageLifecycleRule
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.BucketID,
		&i.RuleID,
		&i.Status,
		&i.Action,
		&i.Days,
		&i.DateAt,
		&i.StorageClass,
		&i.Prefix,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listEnabledStorageLifecycleRules = `-- name: ListEnabledStorageLifecycleRules :many
SELECT id, tenant_id, bucket_id, rule_id, status, action, days, date_at, storage_class, prefix, created_at, updated_at FROM storage_lifecycle_rules
WHERE tenant_id = $1 AND status = 'enabled'
ORDER BY bucket_id ASC, rule_id ASC
`

// : tenant-scoped
// Lists every ENABLED rule across the tenant. Used by the lifecycle
// evaluator River worker on every tick so it can act on due rules in a
// single query per tenant.
func (q *Queries) ListEnabledStorageLifecycleRules(ctx context.Context, tenantID uuid.UUID) ([]StorageLifecycleRule, error) {
	rows, err := q.db.Query(ctx, listEnabledStorageLifecycleRules, tenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []StorageLifecycleRule{}
	for rows.Next() {
		var i StorageLifecycleRule
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.BucketID,
			&i.RuleID,
			&i.Status,
			&i.Action,
			&i.Days,
			&i.DateAt,
			&i.StorageClass,
			&i.Prefix,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listStorageLifecycleRules = `-- name: ListStorageLifecycleRules :many
SELECT id, tenant_id, bucket_id, rule_id, status, action, days, date_at, storage_class, prefix, created_at, updated_at FROM storage_lifecycle_rules
WHERE tenant_id = $1 AND bucket_id = $2
ORDER BY rule_id ASC
LIMIT $3 OFFSET $4
`

type ListStorageLifecycleRulesParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	BucketID uuid.UUID `json:"bucket_id"`
	Limit    int32     `json:"limit"`
	Offset   int32     `json:"offset"`
}

// : tenant-scoped
// Lists every rule attached to the given bucket within the tenant in
// ctx. Ordered by rule_id for deterministic UI rendering.
func (q *Queries) ListStorageLifecycleRules(ctx context.Context, arg ListStorageLifecycleRulesParams) ([]StorageLifecycleRule, error) {
	rows, err := q.db.Query(ctx, listStorageLifecycleRules,
		arg.TenantID,
		arg.BucketID,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []StorageLifecycleRule{}
	for rows.Next() {
		var i StorageLifecycleRule
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.BucketID,
			&i.RuleID,
			&i.Status,
			&i.Action,
			&i.Days,
			&i.DateAt,
			&i.StorageClass,
			&i.Prefix,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setStorageLifecycleRuleStatus = `-- name: SetStorageLifecycleRuleStatus :exec
UPDATE storage_lifecycle_rules
SET status = $3, updated_at = now()
WHERE tenant_id = $1 AND id = $2
`

type SetStorageLifecycleRuleStatusParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	ID       uuid.UUID `json:"id"`
	Status   string    `json:"status"`
}

// : tenant-scoped
// Convenience update for the enable/disable toggle without rewriting
// the rest of the rule. Used by the service layer's Enable/DisableRule
// shortcuts so the audit row metadata can show only the changed field.
func (q *Queries) SetStorageLifecycleRuleStatus(ctx context.Context, arg SetStorageLifecycleRuleStatusParams) error {
	_, err := q.db.Exec(ctx, setStorageLifecycleRuleStatus, arg.TenantID, arg.ID, arg.Status)
	return err
}

const updateStorageLifecycleRule = `-- name: UpdateStorageLifecycleRule :exec
UPDATE storage_lifecycle_rules
SET status = $3,
    days = $4,
    date_at = $5,
    storage_class = $6,
    prefix = $7,
    updated_at = now()
WHERE tenant_id = $1 AND id = $2
`

type UpdateStorageLifecycleRuleParams struct {
	TenantID     uuid.UUID  `json:"tenant_id"`
	ID           uuid.UUID  `json:"id"`
	Status       string     `json:"status"`
	Days         *int32     `json:"days"`
	DateAt       *time.Time `json:"date_at"`
	StorageClass *string    `json:"storage_class"`
	Prefix       *string    `json:"prefix"`
}

// : tenant-scoped
// Replaces the mutable fields of a rule. The rule_id (natural key) is
// immutable; the bucket_id is immutable (a rule cannot hop buckets).
func (q *Queries) UpdateStorageLifecycleRule(ctx context.Context, arg UpdateStorageLifecycleRuleParams) error {
	_, err := q.db.Exec(ctx, updateStorageLifecycleRule,
		arg.TenantID,
		arg.ID,
		arg.Status,
		arg.Days,
		arg.DateAt,
		arg.StorageClass,
		arg.Prefix,
	)
	return err
}
