// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: compute_snapshot_policies.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countComputeSnapshotPolicies = `-- name: CountComputeSnapshotPolicies :one
SELECT count(*) FROM compute_snapshot_policies
WHERE tenant_id = $1 AND deleted_at IS NULL
`

// : tenant-scoped
func (q *Queries) CountComputeSnapshotPolicies(ctx context.Context, tenantID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countComputeSnapshotPolicies, tenantID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createComputeSnapshotPolicy = `-- name: CreateComputeSnapshotPolicy :one

INSERT INTO compute_snapshot_policies (
    tenant_id,
    instance_id,
    name,
    cadence,
    retain_count,
    target_id,
    enabled,
    next_run_at
) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, tenant_id, instance_id, name, cadence, retain_count, target_id, enabled, last_run_at, next_run_at, created_at, updated_at, deleted_at
`

type CreateComputeSnapshotPolicyParams struct {
	TenantID    uuid.UUID  `json:"tenant_id"`
	InstanceID  *uuid.UUID `json:"instance_id"`
	Name        string     `json:"name"`
	Cadence     string     `json:"cadence"`
	RetainCount int32      `json:"retain_count"`
	TargetID    *uuid.UUID `json:"target_id"`
	Enabled     bool       `json:"enabled"`
	NextRunAt   *time.Time `json:"next_run_at"`
}

// compute_snapshot_policies (WS-25): tenant-scoped snapshot schedules.
// Every query here filters by tenant_id (set by WithTenant at the repo seam).
// : tenant-scoped
func (q *Queries) CreateComputeSnapshotPolicy(ctx context.Context, arg CreateComputeSnapshotPolicyParams) (ComputeSnapshotPolicy, error) {
	row := q.db.QueryRow(ctx, createComputeSnapshotPolicy,
		arg.TenantID,
		arg.InstanceID,
		arg.Name,
		arg.Cadence,
		arg.RetainCount,
		arg.TargetID,
		arg.Enabled,
		arg.NextRunAt,
	)
	var i ComputeSnapshotPolicy
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.InstanceID,
		&i.Name,
		&i.Cadence,
		&i.RetainCount,
		&i.TargetID,
		&i.Enabled,
		&i.LastRunAt,
		&i.NextRunAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const getComputeSnapshotPolicyByID = `-- name: GetComputeSnapshotPolicyByID :one
SELECT id, tenant_id, instance_id, name, cadence, retain_count, target_id, enabled, last_run_at, next_run_at, created_at, updated_at, deleted_at FROM compute_snapshot_policies
WHERE tenant_id = $1 AND id = $2 AND deleted_at IS NULL
`

type GetComputeSnapshotPolicyByIDParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	ID       uuid.UUID `json:"id"`
}

// : tenant-scoped
func (q *Queries) GetComputeSnapshotPolicyByID(ctx context.Context, arg GetComputeSnapshotPolicyByIDParams) (ComputeSnapshotPolicy, error) {
	row := q.db.QueryRow(ctx, getComputeSnapshotPolicyByID, arg.TenantID, arg.ID)
	var i ComputeSnapshotPolicy
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.InstanceID,
		&i.Name,
		&i.Cadence,
		&i.RetainCount,
		&i.TargetID,
		&i.Enabled,
		&i.LastRunAt,
		&i.NextRunAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const getComputeSnapshotPolicyByName = `-- name: GetComputeSnapshotPolicyByName :one
SELECT id, tenant_id, instance_id, name, cadence, retain_count, target_id, enabled, last_run_at, next_run_at, created_at, updated_at, deleted_at FROM compute_snapshot_policies
WHERE tenant_id = $1 AND name = $2 AND deleted_at IS NULL
`

type GetComputeSnapshotPolicyByNameParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	Name     string    `json:"name"`
}

// : tenant-scoped
func (q *Queries) GetComputeSnapshotPolicyByName(ctx context.Context, arg GetComputeSnapshotPolicyByNameParams) (ComputeSnapshotPolicy, error) {
	row := q.db.QueryRow(ctx, getComputeSnapshotPolicyByName, arg.TenantID, arg.Name)
	var i ComputeSnapshotPolicy
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.InstanceID,
		&i.Name,
		&i.Cadence,
		&i.RetainCount,
		&i.TargetID,
		&i.Enabled,
		&i.LastRunAt,
		&i.NextRunAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.DeletedAt,
	)
	return i, err
}

const listComputeSnapshotPolicies = `-- name: ListComputeSnapshotPolicies :many
SELECT id, tenant_id, instance_id, name, cadence, retain_count, target_id, enabled, last_run_at, next_run_at, created_at, updated_at, deleted_at FROM compute_snapshot_policies
WHERE tenant_id = $1 AND deleted_at IS NULL
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type ListComputeSnapshotPoliciesParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	Limit    int32     `json:"limit"`
	Offset   int32     `json:"offset"`
}

// : tenant-scoped
func (q *Queries) ListComputeSnapshotPolicies(ctx context.Context, arg ListComputeSnapshotPoliciesParams) ([]ComputeSnapshotPolicy, error) {
	rows, err := q.db.Query(ctx, listComputeSnapshotPolicies, arg.TenantID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ComputeSnapshotPolicy{}
	for rows.Next() {
		var i ComputeSnapshotPolicy
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.InstanceID,
			&i.Name,
			&i.Cadence,
			&i.RetainCount,
			&i.TargetID,
			&i.Enabled,
			&i.LastRunAt,
			&i.NextRunAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listComputeSnapshotPoliciesByInstance = `-- name: ListComputeSnapshotPoliciesByInstance :many
SELECT id, tenant_id, instance_id, name, cadence, retain_count, target_id, enabled, last_run_at, next_run_at, created_at, updated_at, deleted_at FROM compute_snapshot_policies
WHERE tenant_id = $1 AND deleted_at IS NULL
  AND (instance_id = $2 OR instance_id IS NULL)
ORDER BY instance_id NULLS LAST
LIMIT $3 OFFSET $4
`

type ListComputeSnapshotPoliciesByInstanceParams struct {
	TenantID   uuid.UUID  `json:"tenant_id"`
	InstanceID *uuid.UUID `json:"instance_id"`
	Limit      int32      `json:"limit"`
	Offset     int32      `json:"offset"`
}

// : tenant-scoped; returns the per-instance policy (if any) + the tenant-default.
func (q *Queries) ListComputeSnapshotPoliciesByInstance(ctx context.Context, arg ListComputeSnapshotPoliciesByInstanceParams) ([]ComputeSnapshotPolicy, error) {
	rows, err := q.db.Query(ctx, listComputeSnapshotPoliciesByInstance,
		arg.TenantID,
		arg.InstanceID,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ComputeSnapshotPolicy{}
	for rows.Next() {
		var i ComputeSnapshotPolicy
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.InstanceID,
			&i.Name,
			&i.Cadence,
			&i.RetainCount,
			&i.TargetID,
			&i.Enabled,
			&i.LastRunAt,
			&i.NextRunAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listDueComputeSnapshotPolicies = `-- name: ListDueComputeSnapshotPolicies :many
SELECT id, tenant_id, instance_id, name, cadence, retain_count, target_id, enabled, last_run_at, next_run_at, created_at, updated_at, deleted_at FROM compute_snapshot_policies
WHERE deleted_at IS NULL AND enabled = true
  AND next_run_at IS NOT NULL
  AND next_run_at <= $2
ORDER BY next_run_at ASC
LIMIT $1
`

type ListDueComputeSnapshotPoliciesParams struct {
	Limit     int32      `json:"limit"`
	NextRunAt *time.Time `json:"next_run_at"`
}

// : cross-tenant; the take worker scans the whole table for due rows.
// : Tenant scoping is enforced at the worker level (WithTenant is set per
// : row before any DB write). Returns at most $1 rows so the worker
// : processes in bounded batches.
func (q *Queries) ListDueComputeSnapshotPolicies(ctx context.Context, arg ListDueComputeSnapshotPoliciesParams) ([]ComputeSnapshotPolicy, error) {
	rows, err := q.db.Query(ctx, listDueComputeSnapshotPolicies, arg.Limit, arg.NextRunAt)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ComputeSnapshotPolicy{}
	for rows.Next() {
		var i ComputeSnapshotPolicy
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.InstanceID,
			&i.Name,
			&i.Cadence,
			&i.RetainCount,
			&i.TargetID,
			&i.Enabled,
			&i.LastRunAt,
			&i.NextRunAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markComputeSnapshotPolicyRun = `-- name: MarkComputeSnapshotPolicyRun :exec
UPDATE compute_snapshot_policies
SET last_run_at = $3, next_run_at = $4, updated_at = now()
WHERE tenant_id = $1 AND id = $2 AND deleted_at IS NULL
`

type MarkComputeSnapshotPolicyRunParams struct {
	TenantID  uuid.UUID  `json:"tenant_id"`
	ID        uuid.UUID  `json:"id"`
	LastRunAt *time.Time `json:"last_run_at"`
	NextRunAt *time.Time `json:"next_run_at"`
}

// : tenant-scoped; records that the policy just ran at $3 and schedules the
// : next run at $3 + cadence (the caller computes the next_run_at). Used by
// : the take worker after every run, success or failure.
func (q *Queries) MarkComputeSnapshotPolicyRun(ctx context.Context, arg MarkComputeSnapshotPolicyRunParams) error {
	_, err := q.db.Exec(ctx, markComputeSnapshotPolicyRun,
		arg.TenantID,
		arg.ID,
		arg.LastRunAt,
		arg.NextRunAt,
	)
	return err
}

const softDeleteComputeSnapshotPolicy = `-- name: SoftDeleteComputeSnapshotPolicy :exec
UPDATE compute_snapshot_policies
SET deleted_at = now(), enabled = false, updated_at = now()
WHERE tenant_id = $1 AND id = $2 AND deleted_at IS NULL
`

type SoftDeleteComputeSnapshotPolicyParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	ID       uuid.UUID `json:"id"`
}

// : tenant-scoped; marks the row deleted_at=now() + disabled so the worker
// : stops picking it up. Existing snapshots created by this policy stay
// : (their policy_id still points at the row) but are no longer pruned by it.
func (q *Queries) SoftDeleteComputeSnapshotPolicy(ctx context.Context, arg SoftDeleteComputeSnapshotPolicyParams) error {
	_, err := q.db.Exec(ctx, softDeleteComputeSnapshotPolicy, arg.TenantID, arg.ID)
	return err
}

const updateComputeSnapshotPolicy = `-- name: UpdateComputeSnapshotPolicy :exec
UPDATE compute_snapshot_policies
SET instance_id = $3, name = $4, cadence = $5, retain_count = $6,
    target_id = $7, enabled = $8, updated_at = now()
WHERE tenant_id = $1 AND id = $2 AND deleted_at IS NULL
`

type UpdateComputeSnapshotPolicyParams struct {
	TenantID    uuid.UUID  `json:"tenant_id"`
	ID          uuid.UUID  `json:"id"`
	InstanceID  *uuid.UUID `json:"instance_id"`
	Name        string     `json:"name"`
	Cadence     string     `json:"cadence"`
	RetainCount int32      `json:"retain_count"`
	TargetID    *uuid.UUID `json:"target_id"`
	Enabled     bool       `json:"enabled"`
}

// : tenant-scoped; replaces the user-editable fields. The cadence change
// : recomputes next_run_at via MarkComputeSnapshotPolicyDue when the
// : caller wants the new cadence to take effect immediately.
func (q *Queries) UpdateComputeSnapshotPolicy(ctx context.Context, arg UpdateComputeSnapshotPolicyParams) error {
	_, err := q.db.Exec(ctx, updateComputeSnapshotPolicy,
		arg.TenantID,
		arg.ID,
		arg.InstanceID,
		arg.Name,
		arg.Cadence,
		arg.RetainCount,
		arg.TargetID,
		arg.Enabled,
	)
	return err
}
